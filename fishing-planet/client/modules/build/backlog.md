# Backlog — build

- [ ] **Get `ExitOnBuildFailure` into MainClient** — it exists in CodeBranch since r56720 (2026-07-28) but is absent from MainClient, which is the VCS root every console build configuration uses. Until the CodeBranch -> MainClient merge lands, released Xbox/PS4 builds still exit 0 on failure and TeamCity cannot tell a broken build from a good one. Relates: FP-45238 (Xbox fails silently), FP-45257 (PS4 SCEE clone)
- [ ] Extend `ExitOnBuildFailure` to the nine entry points still on the legacy inline handler (PS4 SCEE development, PS4 patch SIEA/SIEE, iOS, Steam, Steam development, Nintendo Release/Development, Epic) — the legacy form exits non-zero only on `BuildResult.Failed`, letting `Cancelled` and `Unknown` report success. Raised by the client lead in FP-45257 as a separate-ticket candidate
- [ ] Consider a CI-side guard independent of the exit code: fail the build when `GDK\Xbox\Package\*.zip` is absent after the Unity step. Catches the case the exit code cannot — a run that packages partially — and gives correct attribution instead of blaming the Nextcloud upload
- [ ] Verify what the Xbox Slack step actually posts to: FP-42415 asked for `#team_city_notification_qa`, while `XBoxProd.kts` posts through a hard-coded webhook and its build feature targets `#team_city_notification_dev`
- [ ] If the Burst toolchain-lookup failure recurs on `BuildClient_NY2`: add Defender exclusion for `C:\ProgramData\Microsoft\VisualStudio` (holds the Setup Configuration native DLL; `bcl.exe` is excluded, the DLL it loads is not). Untested hypothesis — see [xbox_build.md](xbox_build.md)
