# Steam prod fix — audit trail (2026-04-29)

> Concrete IDs, values, and SQL blocks applied to Steam prod on 2026-04-29 to remediate the incident described in [`../journal.md`](../journal.md). Treat this file as an immutable historical record — do not edit prior values, only append corrections at the bottom if errors are later discovered.

The same patterns apply to PS / XB / MOB / NX, but the **values** there must be re-derived from each platform's own `TournamentTemplates` — do not copy the values below verbatim across platforms.

---

## A. Tournament backfill (7 rows in `dbo.Tournaments`)

All 7 future + live competition tournaments whose `ConfigJson` was generated by the previous binary and missed `Places[i].Rating`, `NoShowRatingPenalty`, `ZeroScoreRatingPenalty`, and (for upcoming only) `Grouping`. Time of patch: ~13:00 UTC.

### A.1 Affected tournaments and source-template values

| TournamentId | TemplateId | Template name (i18n) | Phase at patch time | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | R9 | R10 | NoShow | ZeroScore | Grouping patched? |
|--------------|------------|----------------------|---------------------|---:|---:|---:|---:|---:|---:|---:|---:|---:|----:|-------:|----------:|-------------------|
| 318866       | 11842      | (Sport schedule)     | live (12:00–14:00)  | 40 | 35 | 30 | 25 | 20 | 17 | 14 | 12 |  8 |   4 |    -13 |        -7 | **no** (live)     |
| 318867       | 1400       | (Sport schedule)     | reg-open            | 47 | 42 | 37 | 32 | 27 | 22 | 18 | 15 | 11 |   6 |    -15 |        -8 | yes               |
| 318868       | 157        | Cheesy Cat           | reg-open            | 25 | 22 | 20 | 17 | 15 | 12 | 10 |  7 |  5 |   2 |    -10 |        -5 | yes               |
| 318869       | 237        | (Sport schedule)     | reg-open            | 35 | 30 | 25 | 22 | 19 | 15 | 13 | 11 |  7 |   3 |    -11 |        -6 | yes               |
| 318870       | 11874      | (Sport schedule)     | reg-open            | 55 | 50 | 45 | 40 | 35 | 30 | 25 | 20 | 15 |  10 |    -20 |       -10 | yes               |
| 318871       | 1500       | (Sport schedule)     | reg-open            | 55 | 50 | 45 | 40 | 35 | 30 | 25 | 20 | 15 |  10 |    -20 |       -10 | yes               |
| 318872       | 11881      | (Sport schedule)     | reg-open            | 55 | 50 | 45 | 40 | 35 | 30 | 25 | 20 | 15 |  10 |    -20 |       -10 | yes               |

Distinct registered users on these 7 at patch time: **268**. Total registration rows: **506** (179 on live 318866 + 327 across the six future). The "Grouping patched? = no" entry for 318866 reflects the deliberate exclusion in section A.4 below — `ProcessGrouping` had already run for the live tournament; rating math equivalent due to `RatingMultiplier = 1.0`.

### A.2 SQL block — Places[i].Rating backfill (all 7)

```sql
BEGIN TRAN;

;WITH RatingMap (TournamentId, R1, R2, R3, R4, R5, R6, R7, R8, R9, R10) AS (
    SELECT * FROM (VALUES
        (318866, 40, 35, 30, 25, 20, 17, 14, 12,  8,  4),
        (318867, 47, 42, 37, 32, 27, 22, 18, 15, 11,  6),
        (318868, 25, 22, 20, 17, 15, 12, 10,  7,  5,  2),
        (318869, 35, 30, 25, 22, 19, 15, 13, 11,  7,  3),
        (318870, 55, 50, 45, 40, 35, 30, 25, 20, 15, 10),
        (318871, 55, 50, 45, 40, 35, 30, 25, 20, 15, 10),
        (318872, 55, 50, 45, 40, 35, 30, 25, 20, 15, 10)
    ) v(TournamentId, R1, R2, R3, R4, R5, R6, R7, R8, R9, R10)
)
UPDATE t
SET ConfigJson =
    JSON_MODIFY(JSON_MODIFY(JSON_MODIFY(JSON_MODIFY(JSON_MODIFY(
    JSON_MODIFY(JSON_MODIFY(JSON_MODIFY(JSON_MODIFY(JSON_MODIFY(
        t.ConfigJson,
        '$.Places[0].Rating', rm.R1),
        '$.Places[1].Rating', rm.R2),
        '$.Places[2].Rating', rm.R3),
        '$.Places[3].Rating', rm.R4),
        '$.Places[4].Rating', rm.R5),
        '$.Places[5].Rating', rm.R6),
        '$.Places[6].Rating', rm.R7),
        '$.Places[7].Rating', rm.R8),
        '$.Places[8].Rating', rm.R9),
        '$.Places[9].Rating', rm.R10)
OUTPUT
    inserted.TournamentId,
    LEN(deleted.ConfigJson)  AS OldLen,
    LEN(inserted.ConfigJson) AS NewLen,
    JSON_VALUE(inserted.ConfigJson, '$.Places[0].Rating') AS NewR1,
    JSON_VALUE(inserted.ConfigJson, '$.Places[9].Rating') AS NewR10
FROM dbo.Tournaments t
JOIN RatingMap rm ON rm.TournamentId = t.TournamentId
WHERE t.KindId = 3;

-- Verify OUTPUT (7 rows; OldLen + ~115 ≈ NewLen; NewR1/NewR10 match the matrix)
COMMIT;
```

### A.3 SQL block — root-level penalty fields backfill (all 7)

```sql
BEGIN TRAN;

;WITH PenaltyMap (TournamentId, NoShow, ZeroScore) AS (
    SELECT * FROM (VALUES
        (318866, -13,  -7),
        (318867, -15,  -8),
        (318868, -10,  -5),
        (318869, -11,  -6),
        (318870, -20, -10),
        (318871, -20, -10),
        (318872, -20, -10)
    ) v(TournamentId, NoShow, ZeroScore)
)
UPDATE t
SET ConfigJson =
    JSON_MODIFY(
    JSON_MODIFY(
        t.ConfigJson,
        '$.NoShowRatingPenalty',    pm.NoShow),
        '$.ZeroScoreRatingPenalty', pm.ZeroScore)
OUTPUT
    inserted.TournamentId,
    LEN(deleted.ConfigJson)  AS OldLen,
    LEN(inserted.ConfigJson) AS NewLen,
    JSON_VALUE(inserted.ConfigJson, '$.NoShowRatingPenalty')    AS NewNoShow,
    JSON_VALUE(inserted.ConfigJson, '$.ZeroScoreRatingPenalty') AS NewZeroScore
FROM dbo.Tournaments t
JOIN PenaltyMap pm ON pm.TournamentId = t.TournamentId
WHERE t.KindId = 3;

-- Verify OUTPUT (7 rows; ~+54 bytes per row; NewNoShow/NewZeroScore match the matrix)
COMMIT;
```

### A.4 SQL block — Grouping backfill (6 upcoming, live excluded)

`Grouping` was copied from each tournament's source template (`TournamentTemplates.ConfigJson`). Live tournament 318866 was deliberately skipped — `ProcessGrouping` had already executed (no `BracketId` to assign retroactively), and `RatingMultiplier = 1.0` in all source templates means rating math is identical with or without `Grouping` in the live config.

```sql
BEGIN TRAN;

UPDATE t
SET ConfigJson = JSON_MODIFY(
        t.ConfigJson,
        '$.Grouping',
        JSON_QUERY(tt.ConfigJson, '$.Grouping')
    )
OUTPUT
    inserted.TournamentId,
    LEN(deleted.ConfigJson)  AS OldLen,
    LEN(inserted.ConfigJson) AS NewLen,
    JSON_VALUE(inserted.ConfigJson, '$.Grouping.MinSize')                 AS NewMinSize,
    JSON_VALUE(inserted.ConfigJson, '$.Grouping.Brackets[0].BracketName') AS NewBr1,
    JSON_VALUE(inserted.ConfigJson, '$.Grouping.Brackets[1].BracketName') AS NewBr2,
    JSON_VALUE(inserted.ConfigJson, '$.Grouping.Brackets[2].BracketName') AS NewBr3
FROM dbo.Tournaments t
JOIN dbo.TournamentTemplates tt WITH (NOLOCK) ON tt.TemplateId = t.TemplateId
WHERE t.TournamentId IN (318867, 318868, 318869, 318870, 318871, 318872)
  AND t.KindId = 3
  AND JSON_QUERY(tt.ConfigJson, '$.Grouping') IS NOT NULL;

-- Verify OUTPUT (6 rows; +~430 bytes per row; MinSize=20; Brackets Newbies/Midles/Tops with MinRating 0/101/1001)
COMMIT;
```

---

## B. Template 181 + Tournament 320415 — `Place 7` Rating fill

Content defect: `Place 7` had no `Rating` in template 181 ("Triple the Trout!"). Tournament 320415 (TemplateId 181, scheduled 2026-05-06 06:00 UTC) inherited the gap. Design provided `Rating = 13`. Both rows updated in a single transaction.

```sql
BEGIN TRAN;

UPDATE dbo.TournamentTemplates
SET ConfigJson = JSON_MODIFY(ConfigJson, '$.Places[6].Rating', 13)
OUTPUT 'Template' AS Side, inserted.TemplateId,
       LEN(deleted.ConfigJson)  AS OldLen,
       LEN(inserted.ConfigJson) AS NewLen,
       JSON_VALUE(inserted.ConfigJson, '$.Places[6].PlaceId') AS NewPlaceId,
       JSON_VALUE(inserted.ConfigJson, '$.Places[6].Rating')  AS NewRating
WHERE TemplateId = 181
  AND JSON_VALUE(ConfigJson, '$.Places[6].PlaceId') = '7'
  AND JSON_VALUE(ConfigJson, '$.Places[6].Rating') IS NULL;

UPDATE dbo.Tournaments
SET ConfigJson = JSON_MODIFY(ConfigJson, '$.Places[6].Rating', 13)
OUTPUT 'Tournament' AS Side, inserted.TournamentId,
       LEN(deleted.ConfigJson)  AS OldLen,
       LEN(inserted.ConfigJson) AS NewLen,
       JSON_VALUE(inserted.ConfigJson, '$.Places[6].PlaceId') AS NewPlaceId,
       JSON_VALUE(inserted.ConfigJson, '$.Places[6].Rating')  AS NewRating
WHERE TournamentId = 320415
  AND KindId = 3
  AND JSON_VALUE(ConfigJson, '$.Places[6].PlaceId') = '7'
  AND JSON_VALUE(ConfigJson, '$.Places[6].Rating') IS NULL;

-- Verify OUTPUT (2 rows: Template + Tournament, NewPlaceId=7, NewRating=13)
COMMIT;
```

---

## C. FinishPoint normalisation (15 templates)

Content defect: 15 active templates carry an RFC-invalid `FinishPoint` block — JavaScript object literal with unquoted property names: `{ X: 307, Y: 0, Z: 596 }`. Pattern was identical across all 15 (whitespace `{ X:` / `, Y:` / `, Z:`). Fixed via idempotent `REPLACE`.

### C.1 Affected templates

| TemplateId | KindId    | Notes                        |
|------------|-----------|------------------------------|
| 1320       | 1 (Sport) | tournament-style template    |
| 1340       | 1         | tournament-style template    |
| 1350       | 1         | tournament-style template    |
| 1360       | 1         | tournament-style template    |
| 1370       | 1         | tournament-style template    |
| 11800      | 1         | tournament-style template    |
| 11810      | 1         | tournament-style template    |
| 11820      | 1         | tournament-style template    |
| 11830      | 1         | tournament-style template    |
| 11840      | 1         | tournament-style template    |
| 11848      | 3 (Comp.) | "Marble Frenzy on the Tiber" |
| 11849      | 3         | "Bass Speed Hunt"            |
| 11850      | 3         | "Falcon Trout Chase"         |
| 11851      | 3         | "Smile, it's Jacunda time!"  |
| 11852      | 3         | "A Truly Unique Race!"       |

### C.2 SQL block

```sql
UPDATE dbo.TournamentTemplates
SET ConfigJson =
    REPLACE(REPLACE(REPLACE(
        ConfigJson,
        '{ X:', '{ "X":'),
        ', Y:', ', "Y":'),
        ', Z:', ', "Z":')
WHERE ConfigJson LIKE N'%FinishPoint%'
  AND ISJSON(ConfigJson) = 0;
```

Result: 15 rows affected, 295 ms. Verified post-patch: `HasFinishPoint=15`, `StillInvalid=0`, `NowValid=15`, no residual `{ X:` / `, Y:` / `, Z:` markers anywhere in `TournamentTemplates`.

---

## Verification

After all patches above were applied, a phase-summary scan of Steam prod returned a clean state across `live`, `registration-open`, `reg-opens-soon`, and `far-future` phases — every `Missing*` counter at zero:

| Phase             | Total | MissingPlaceRating | MissingNoShow | MissingZeroScore | MissingGrouping |
|-------------------|-------|--------------------|---------------|------------------|-----------------|
| live              | 1     | 0                  | 0             | 0                | 0               |
| registration-open | 6     | 0                  | 0             | 0                | 0               |
| reg-opens-soon    | 1     | 0                  | 0             | 0                | 0               |
| far-future        | 139   | 0                  | 0             | 0                | 0               |

(Live 318866 was already in `past` by the time of the final verification — its `Grouping`-missing state was therefore not visible in the live row.)

The `far-future` total drifts between scans on the same evening (`139` here, `138` in the post-`MaxRating` patch scan in section D.7, `142` quoted in the matchmaking-module log entry's earlier snapshot). It is not a discrepancy: the scheduling pipeline keeps generating new `far-future` rows continuously, and the sliding `now`-relative phase boundaries move with each scan. All three numbers are correct for the moment they were captured.

Templates were also clean: no content defects (no Place without Rating, both penalty fields present, valid JSON across the active competition cohort).

---

## D. Brackets[i].MaxRating backfill — follow-up to section A.4 (2026-04-29 evening)

Section A.4 above (`Grouping` copy from each tournament's source template via `JSON_QUERY` + `JSON_MODIFY`) turned out to be incomplete: the resulting `Grouping` object lacked `Brackets[i].MaxRating` for every bracket, because templates never store `MaxRating` — it is auto-computed at runtime by `MatchmakingLogic.InitializeGrouping` during template deserialization. The defect surfaced as 318868, 318869, 318870 reached `StartDate` and `ProcessGrouping` ran on the half-initialized object, leaving roughly two-thirds of registered participants without a `BracketId` / `GroupName`. This section records the diagnosis, the second-round backfill applied to the two still-upcoming competitions, the rationale for leaving the three already-running competitions as-is, and a `MaxRating`-aware verification scan.

### D.1 Why MaxRating ended up missing

`MatchmakingLogic.cs` ➜ `CreateBuckets` filters participants by `p.CompetitionRating >= bucket.MinRating && p.CompetitionRating <= bucket.MaxRating`. `MaxRating` is auto-computed by `MatchmakingLogic.InitializeGrouping`, which is invoked only from `TournamentsHelper.FromDto(TournamentTemplateDto)` — the template-side deserializer. The companion `TournamentsHelper.FromDto(TournamentDto)` (used at competition start by `TournamentStartAdapter.StartTournamentAsync` ➜ `MatchmakingLogic.ProcessGrouping`) does **not** call `InitializeGrouping`. The regular generation pipeline (`ScheduleCompetitions` / `RandomizeCompetitions`) sidesteps the asymmetry by deserializing the source template first (which initializes `Grouping`), then re-serializing the now-initialized object into the Tournament's `ConfigJson` — so `MaxRating` ends up persisted on disk by the time `ProcessGrouping` reads the row back.

Section A.4 broke that invariant: `JSON_QUERY(tt.ConfigJson, '$.Grouping')` returned the template's raw `Grouping` (no `MaxRating` — never stored there) and `JSON_MODIFY` placed it into `Tournaments.ConfigJson` verbatim. At competition start, `int MaxRating` defaulted to `0` for every bracket, so:

- `Brackets[0]` (Newbies, `MinRating=0`): admitted only `CompetitionRating == 0` (including `NULL`, which maps to `0` in `int`).
- `Brackets[1]` (Midles, `MinRating=101`): inverted range `[101..0]`, empty.
- `Brackets[2]` (Tops, `MinRating=1001`): inverted range `[1001..0]`, empty.

`BalanceBuckets` had nothing to redistribute; `BuildGroups` produced exactly one group `A` in bucket 1; `AssignGroupsToParticipants` updated only those participants, leaving the rest with `BracketId IS NULL`, `GroupName IS NULL`.

### D.2 Observed defect (Steam prod, queried 2026-04-29 ~22:00 UTC)

| TournamentId | TemplateId | Phase at observation                                | Total reg | Assigned | All assigned to    | Unassigned |
|--------------|------------|-----------------------------------------------------|-----------|----------|--------------------|------------|
| 318868       | 157        | ended                                               | 222       | 78 (35%) | bracket 1, group A | 144        |
| 318869       | 237        | ended                                               | 227       | 58 (26%) | bracket 1, group A | 169        |
| 318870       | 11874      | live (StartDate 04-29 20:00 UTC)                    | 83        | 37 (45%) | bracket 1, group A | 46         |
| 318871       | 1500       | reg-open (StartDate 04-29 22:00 UTC, 60 min ahead)  | 84        | 0        | —                  | 84         |
| 318872       | 11881      | reg-open (StartDate 04-30 00:00 UTC, 180 min ahead) | 22        | 0        | —                  | 22         |

Cross-checked: in each of the three "already grouped" tournaments, the assigned cohort had `CompetitionRating` of `0` or `NULL` *at the time `ProcessGrouping` ran* (current `Profiles.CompetitionRating` for those users sits between `0` and `35`, the small spread reflecting rating earned during the very competition under inspection — i.e. post-grouping mutations). The unassigned cohort spans all three rating bands (`< 101`, `101..1000`, `>= 1001`), confirming the "MaxRating == 0" hypothesis: neither bucket 2 nor bucket 3 ever received a single participant, and even bracket-1-eligible participants with `rating > 0` were filtered out by the inverted range guard.

### D.3 Scope of this patch

Backfill applied to **318871 and 318872 only**. Rationale per ID:

| TournamentId | At patch time                  | Action   | Why                                                                                                                                                                                                                                                                                     |
|--------------|--------------------------------|----------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 318868       | already ended                  | none     | `ProcessGrouping` already ran; ConfigJson edit cannot retroactively redistribute participants. `RatingMultiplier = 1.0` on every bracket in the source template makes per-bracket rating math identical with or without the split — same calculus as the live exclusion in section A.4. |
| 318869       | already ended                  | none     | same                                                                                                                                                                                                                                                                                    |
| 318870       | live (already past StartDate)  | none     | same — `ProcessGrouping` already ran. Editing the live ConfigJson would not reissue assignments.                                                                                                                                                                                        |
| 318871       | reg-open, 60 min to StartDate  | backfill | StartDate well outside the AsyncProcessor 5-minute "soon" window (`TournamentStartAdapter.SoonTimeout`), so `GetTournamentsToStartSoon` had not yet returned a DTO into a worker thread — a subsequent poll picks up the patched ConfigJson before `ProcessGrouping` runs.              |
| 318872       | reg-open, 180 min to StartDate | backfill | same                                                                                                                                                                                                                                                                                    |

Verified independently that the AsyncProcessor path reads `ConfigJson` live from the SP `GetTournamentsToStartSoon` — no Tournament-DTO-level cache (`Shared/SharedLib/Config/TournamentsCache.cs` covers `TournamentSeries`, not `Tournaments`). The only "freezing" point is the local variable inside `StartTournamentAsync` for the duration of its `Thread.Sleep` poll until `StartDate`, which begins at most 5 minutes ahead.

### D.4 Target values

`MaxRating[i]` recomputed by hand using the same rule `InitializeGrouping` applies (brackets sorted ascending by `MinRating`; `MaxRating[i] = MinRating[i+1] - 1`; last bracket = `int.MaxValue`):

| Bracket index (0-based) | BracketName | MinRating | MaxRating  |
|-------------------------|-------------|-----------|------------|
| 0                       | Newbies     | 0         | 100        |
| 1                       | Midles      | 101       | 1000       |
| 2                       | Tops        | 1001      | 2147483647 |

These match exactly what every `far-future` competition (`TournamentId >= 320342`) already carries in its `ConfigJson` — the regular generation pipeline writes them via the path described in D.1.

### D.5 SQL block (applied)

```sql
-- Pre-check: expect MaxRating=NULL for both rows, BracketName/MinRating shape sane
SELECT
    t.TournamentId,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[0].BracketName')  AS Br1_Name,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[0].MinRating')    AS Br1_Min,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[0].MaxRating')    AS Br1_Max,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[1].BracketName')  AS Br2_Name,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[1].MinRating')    AS Br2_Min,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[1].MaxRating')    AS Br2_Max,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[2].BracketName')  AS Br3_Name,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[2].MinRating')    AS Br3_Min,
    JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[2].MaxRating')    AS Br3_Max,
    LEN(t.ConfigJson) AS Len
FROM dbo.Tournaments t WITH (NOLOCK)
WHERE t.TournamentId IN (318871, 318872)
ORDER BY t.TournamentId;

BEGIN TRAN;

UPDATE t
SET ConfigJson =
    JSON_MODIFY(
    JSON_MODIFY(
    JSON_MODIFY(
        t.ConfigJson,
        '$.Grouping.Brackets[0].MaxRating', 100),
        '$.Grouping.Brackets[1].MaxRating', 1000),
        '$.Grouping.Brackets[2].MaxRating', 2147483647)
OUTPUT
    inserted.TournamentId,
    LEN(deleted.ConfigJson)  AS OldLen,
    LEN(inserted.ConfigJson) AS NewLen,
    JSON_VALUE(inserted.ConfigJson, '$.Grouping.Brackets[0].MaxRating') AS NewBr1_Max,
    JSON_VALUE(inserted.ConfigJson, '$.Grouping.Brackets[1].MaxRating') AS NewBr2_Max,
    JSON_VALUE(inserted.ConfigJson, '$.Grouping.Brackets[2].MaxRating') AS NewBr3_Max
FROM dbo.Tournaments t
WHERE t.TournamentId IN (318871, 318872)
  AND t.KindId = 3
  AND JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[0].MaxRating') IS NULL  -- idempotency guard
  AND JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[0].MinRating') = '0'    -- shape sanity
  AND JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[1].MinRating') = '101'
  AND JSON_VALUE(t.ConfigJson, '$.Grouping.Brackets[2].MinRating') = '1001';

-- Verify OUTPUT (2 rows; ~+56 bytes per row; NewBrN_Max = 100 / 1000 / 2147483647)
COMMIT;
```

The 3-bracket layout is hard-coded — appropriate for Steam where every active Competition template happens to use the same Newbies/Midles/Tops shape. For cross-platform reuse, see "Open threads" — the unified release-time script must recompute `MaxRating` dynamically from `MinRating[i+1] - 1` to be schema-agnostic.

### D.6 Post-patch row state

| TournamentId | MinSize | Br1 (Newbies)              | Br2 (Midles)                  | Br3 (Tops)                           | Len  |
|--------------|---------|----------------------------|-------------------------------|--------------------------------------|------|
| 318871       | 20      | MinRating=0, MaxRating=100 | MinRating=101, MaxRating=1000 | MinRating=1001, MaxRating=2147483647 | 2580 |
| 318872       | 20      | MinRating=0, MaxRating=100 | MinRating=101, MaxRating=1000 | MinRating=1001, MaxRating=2147483647 | 2323 |

Identical to the shape carried by every healthy `TournamentId >= 320342`.

### D.7 Phase summary after the patch (MaxRating-aware)

A bespoke `MaxRating`-aware scan was run shortly after `COMMIT` to confirm that the per-bracket `MaxRating` was now present everywhere it should be:

| Phase             | Total | NoGroupingObject | GroupingPresentButMaxMissing | Healthy |
|-------------------|-------|------------------|------------------------------|---------|
| 1-live            | 1     | 0                | **1**                        | 0       |
| 2-reg-open        | 6     | 0                | 0                            | 6       |
| 3-reg-opens-soon  | 1     | 0                | 0                            | 1       |
| 4-far-future      | 138   | 0                | 0                            | 138     |

The single `MaxMissing` row in the live phase is **318870**, deliberately left alone (see D.3). Drill-down query confirms `318870` is the only outlier and that `JSON_QUERY(t.ConfigJson, '$.Grouping') IS NOT NULL` for it (object is present, only the per-bracket `MaxRating` is missing — the exact pattern this section's defect class is about).

---

---

## E. Generalised fix-up script (post-incident)

All hand-crafted patches in sections A through D have been distilled into a single idempotent T-SQL script:

`SQL/Releases/R202604-Leaderboards-Rating-Matchmaking-FixUp.sql`

It is the deliverable for replaying the same fix sequence on PS / XB / MOB / NX during their respective release downtimes, plus a re-runnable health-and-fix tool for any future similar incident on Steam.

### E.1 What the script automates

| Section above                                                                                      | What the script does                                                                                                                                                                                               |
|----------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| C (FinishPoint normalisation)                                                                      | Phase 0: REPLACE `{ X:` / `, Y:` / `, Z:` with quoted equivalents on every `TournamentTemplates` row whose `ConfigJson LIKE '%FinishPoint%' AND ISJSON = 0`. Idempotent.                                           |
| A.1, A.2, A.3 (`Places[i].Rating` 1..10, `NoShowRatingPenalty`, `ZeroScoreRatingPenalty` backfill) | Phase 2 steps 2.3–2.5: copy values from each Tournament's source template, with `IS NULL` guards.                                                                                                                  |
| A.4 (`Grouping` object copy from template)                                                         | Phase 2 step 2.1.                                                                                                                                                                                                  |
| D (`Brackets[i].MaxRating` recompute)                                                              | Phase 2 step 2.2: `LEAD` over `MinRating` ascending, `MaxRating[i] = MinRating[i+1] - 1`, last bracket = `2147483647` — schema-agnostic, no hard-coded 3-bracket layout from D.5.                                  |
| Verification scan from this artifact's "Verification" section                                      | Phase 3: same shape, plus `MissingMaxRating` column. Result is checked by a verify-before-commit gate that refuses to persist Phase 2 if any `Missing*` is non-zero.                                               |
| Section B fix (template 181 `Place 7 = 13`)                                                        | **Not generalised** — content defects require human input. The script detects them in Phase 1.1 and aborts with a report; design must fix the template (in WebAdmin or via point `JSON_MODIFY`) before re-running. |

### E.2 What the script does not address

- **Already-grouped tournaments with `MaxRating` gap.** Section D.3 left 318868 / 318869 / 318870 untouched because `ProcessGrouping` had already executed; same rationale applies on any platform where a similar window happens. The script's Phase 2 filter (`IsStarted=0 AND IsEnded=0 AND IsCanceled=0 AND IsDeleted=0 AND StartDate >= now+5m`) intentionally excludes such tournaments — Phase 1.2 / 1.3 would surface them as anomalies and abort.
- **Code-side asymmetry between the two `TournamentsHelper.FromDto` overloads** (template vs tournament). The script accepts the implicit "`MaxRating` must be persisted in `ConfigJson`" contract as given and writes the field; it does not make the runtime self-healing. Closing the gap structurally is a separate code change — see "Outstanding code-side fix" below.

### E.3 Local smoke-test outcome (2026-05-06)

The script was validated end-to-end on the local development DB after restoring it to a pre-fix-up state:

- Phase 0 normalised 15 templates with unquoted `FinishPoint` (committed).
- Phase 1.1 surfaced template 181 (`Place 7` without `Rating`) — the same content defect found on Steam prod. Manually fixed via point `JSON_MODIFY` (per section B).
- Re-run with `@DryRun = 0`: Phase 2 wrote Place-level `Rating` to 2 tournaments (the local instances generated from template 181), every other `2.x` step a no-op (147 tournaments were already healthy because they had been generated by the post-release binary). Phase 3 verification gate reported zero gaps and committed.

Confirms the abort gate (template defect → halt) and the happy path (all writes idempotent, verify-before-commit) both behave as designed.

### E.4 Outstanding code-side fix (related but out of this artifact's scope)

The asymmetry between `TournamentsHelper.FromDto(TournamentTemplateDto)` (calls `MatchmakingLogic.InitializeGrouping`) and `FromDto(TournamentDto)` (does not) is the structural reason the manual section-A.4 step was incomplete and required section D as a follow-up. Two minimally invasive code-level options:

- **Local fix:** in `MatchmakingLogic.ProcessGroupingForTournament` (the private overload that already receives the `Tournament` post-FromDto), insert `MatchmakingLogic.InitializeGrouping(tournament.Grouping)` immediately after the early-return guard `if (tournament.KindId != Competition || tournament.Grouping == null) return new TournamentGroup[] { }`. Closes the gap exactly where it manifests; no other call site is affected.
- **Symmetric fix:** add `MatchmakingLogic.InitializeGrouping(result.Grouping)` to `FromDto(TournamentDto)`, mirroring `FromDto(TournamentTemplateDto)`. Closes the gap for any future caller as well.

Either option turns the implicit "`MaxRating` must be persisted on disk" contract into a self-healing one — `JSON_MODIFY` backfills that copy `Grouping` from a template can stop worrying about `MaxRating`, and the runtime works correctly even if `MaxRating` was never written. **Strongly recommended to land before the next platform deploy** (PS/XB/MOB/NX); otherwise the fix-up script's step 2.2 remains essential. Tracked in the task backlog.
