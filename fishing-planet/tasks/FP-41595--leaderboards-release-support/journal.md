---
status: in-progress
executor: Stanislav Samoilov
jira: https://fishingplanet.atlassian.net/browse/FP-41595
parent: FP-26788
---
# FP-41595: Leaderboards — Server Release Support & Bugfix

## Status
In progress (transitioned 2026-04-27). Umbrella task for any LBM20251201 release-prep work that does not belong to a dedicated JIRA task — investigations, audits, sanity checks, ad-hoc fixes, configuration reviews. JIRA title and original description are scoped to leaderboards, but in practice the ticket is used as a catch-all bucket for release-readiness work.

## Summary
Catch-all task for release-prep work outside other tickets. Whatever surfaces while preparing the LBM branch for production and is not already tracked by a feature/bug ticket lands here: data migration validation, schema/config sanity checks, cross-branch deltas, one-off fixes uncovered during the release cycle. Each line of work gets its own artifact under `artifacts/`; the journal records milestones.

First focus area (2026-04-27): cross-branch audit of non-migrated tables (`AbTests`, `EnvironmentVariables`) to confirm every variable and A/B test introduced since the GRM20240409 branch (r11916) is delivered to prod via SQL patches, with correct default values.

## Plan
No formal multi-phase plan — work is driven by issues as they surface. Active directions:
- [AbTests & EnvironmentVariables audit](artifacts/abtests-and-envvars-audit.md) — done 2026-04-27
- [Release checklist content (paste-ready)](artifacts/release-checklist-content.md) — drafted 2026-04-27, all rows ⚠ TBD pending owner verification 2026-04-28

## Milestones
- 2026-04-27: Task transitioned to In Progress; KB journal created
- 2026-04-27: Completed audit of `AbTests` (7 entries since GRM, IDs 16–22) and `EnvironmentVariables` (17 SQL patches affecting these rows) — every change mapped to JIRA ticket; LBM-specific delta isolated for STEAM/PS/Xbox migration path; report at `artifacts/abtests-and-envvars-audit.md`
- 2026-04-27: Pulled desired prod values from GDDs/release checklist ([Leaderboards GDD](https://fishingplanet.atlassian.net/wiki/spaces/FP/pages/4532731906/Leaderboards+GDD+-+1st+iteration), [Matchmaking GDD](https://fishingplanet.atlassian.net/wiki/spaces/FP/pages/4067721271/MatchMaking+System+-+1st+Iteration+GDD), [Rating GDD](https://fishingplanet.atlassian.net/wiki/spaces/FP/pages/4104945682/New+competition+rating+system+GDD+-+1st+iteration), [DM GDD](https://fishingplanet.atlassian.net/wiki/spaces/FP/pages/5351309314/Daily+Missions+GDD+2.0), [Release checklist](https://fishingplanet.atlassian.net/wiki/spaces/FP/pages/5403607042/2026.3+-+Leaderboards+Matchmaking+DailyMissions+Server+Release+checklist)). Added "Desired prod values per GDDs" section to the audit artifact. Key takeaway: 13 `Is*On` Leaderboards flags are inserted as `N` but must be flipped to `Y` for the feature to function; `IsRatingByPlaceEnabled` similarly inserted `N`, must be `Y`; Daily Missions feature flag `IsDailyMissionsOn` must flip to `Y` at launch. Also confirmed `EnvironmentVariableCache.RemovePrefix` strips the first dotted segment, so `Leaderboards.X` and bare `X` resolve to the same code-side lookup
- 2026-04-27: Restructured the audit artifact into per-feature breakdown (LB / MM / Rating / DM) with `Action = auto/flip/verify` columns ready for checklist conversion. Pulled an explicit "Quick-reference: rollout flip list" — 15 EV flips total for the four in-scope features. Items not covered by these features (Personal Offers A/B tests, ping/connectivity flags, `FishStats.*`, `DetailedAnalyticsLoggingMaxLevel`, `UseTrackedDelivery`, etc.) moved to "Out of immediate focus" section; separate audit pass tracked in backlog
- 2026-04-27: Cataloged out-of-focus EVs/A/B tests into themed groups in audit § Other features — Connectivity/Network (6 EV), Stats/Analytics (3 EV), Account/Identity (2 EV), Anti-cheat (1 deletion), Product delivery (1 EV), Personal Offers A/B tests (4 tests), FTUE A/B test (1 test), one-time data cleanup. `verify`/`investigate` actions marked per row; remaining work — resolve TBD desired-prod values for `investigate` rows (Ping thresholds, Connectivity flag, `UseNewUserSearch`, `UseTrackedDelivery`) — logged in backlog
- 2026-04-27: Linkified JIRA IDs and Confluence page references throughout journal/backlog/audit artifact
- 2026-04-27: Ran audit SQL on STEAM PROD (KNW + LBM-001/LBM-010 cherry-picks). All EV/GV/AbTests checks match expectations once accounting for: (a) prod is at KNW-level so non-cherry-picked LBM rows are correctly absent; (b) GV will be filled via QA→PROD sync at rollout, not via LBM-002 patch; (c) `UseNewUserSearch=1` is intentional post-Saltwater activation (gold); (d) `FishStats.CollectFishGenerationStats='Y'` is functionally `'1'` (`GetBoolValue` accepts Y/N or 1/0); (e) A/B tests 16/19/20 prod values are product-managed gold standard
- 2026-04-27: Clarified A/B test runtime semantics with user — `IsActive=false, DefaultValue=true` is a global override (feature ON for all), not "test deployed but disabled". Updated audit § A/B Tests with semantics table + revised "Prod current → Desired post-LBM" columns for tests 16/17/18/19/20 (preserve as-is) vs 21/22 (insert via LBM patches; later activation is product decision). Closed three backlog items (verify EV rows on prod, confirm runtime feature-flag values, cross-check AbTests semantics)
- 2026-04-27: Reframed audit per user's clarification — every EV/A/B test row gets a "Patch insert | Prod current | Action at LBM release" classification; nothing is "gold standard preserve" automatically. Added consolidated **Rollout checklist** (Group 1: 14 explicit Set-`Y` actions; Group 2: 10 TBDs to ask owners; Group 3: 7 leave/verify no-op items). Removed loaded "gold" terminology throughout. Built initial checklist; TBDs go to owners next
- 2026-04-27: QA snapshot read on full LBM (up to LBM-041). Findings: (1) all 13 LB `Is*On` flags + `IsRatingByPlaceEnabled` are at `Y` on QA — confirms Group 1 actions; (2) **`IsDailyMissionsOn=Y` on QA** — strong signal DM ships with LBM; promoted from Group 2 TBD to Group 1 (Set `Y`, pending DM team final confirm); (3) all other Group 2 EVs (Ping/Connectivity/UseTrackedDelivery/DetailedAnalyticsLoggingMaxLevel) at QA = patch defaults — suggests "leave inactive at rollout"; (4) Tests 21/22 inserted on QA with patch defaults, not activated — suggests leave inactive at rollout; (5) LBM-002 GV migration ran cleanly on QA (6 rows in GV, EV-absent ✓); (6) **QA≠PROD discrepancies** noted: `UseNewUserSearch` (QA=0, PROD=1; QA not synced) and Test 18 `Probability` (QA=0.5, PROD=0 — functionally moot since `IsActive=false`). Updated all per-feature tables with QA column; rollout checklist now has QA evidence per row
- 2026-04-27: Drafted [release-checklist-content.md](artifacts/release-checklist-content.md) — paste-ready Confluence content for the release checklist step "Environment Variables and AB Tests". Two sections: (1) LB/MM/Rating/DM = 15 EV flips + Test 21 + JsonVar `AbTestMinLevel`, all ⚠ TBD pending feature-owner confirmation 2026-04-28; (2) Other variables, split into "existing on prod (preserve)" and "new at rollout (`UseTrackedDelivery`, Test 22 — both ⚠ TBD)". Format follows the example in current Confluence checklist (`Variable = Value` for EVs; `Platform #TestId (Name) — Default Value bool (bit), Enabled bool (bit)` for tests)
- 2026-04-27: Tightened scope per user — `GlobalVariables` and `JsonVariables` are fully replaced by QA→PROD DataPump at release (checklist step "Transfer data updates from QA"); their final prod state ≡ QA state. Only EV/AbTests need entries in the rollout-action checklist. Removed `AbTestMinLevel` JsonVariable subsection from `release-checklist-content.md` (auto-handled by QA sync, no human action required). GV references in audit kept only for the 6 LBM-002 EV→GV migrated rows (the EV-side delete is patch-driven; the GV-side appears via QA sync, not via the patch — patch's GV insert is a no-op overwritten by sync). Added explicit "Out of scope: GV/JV" note to audit intro
- 2026-04-27: GD requirements confirmed for paste-ready Confluence cell content. Decisions: **Leaderboards fully disabled** (all 13 LB `Is*On` + `IsLeaderboardsOn` = `N`, contrary to QA which had them all `Y`); **Rating/Matchmaking enabled** (`IsRatingByPlaceEnabled` = `Y`); **DM enabled** (`IsDailyMissionsOn` = `Y`); **`UseTrackedDelivery` disabled** (`false`); **new A/B tests 21/22 inactive with `DefaultValue=false`** (return false for all players). Added "★ Paste this into Confluence cell" section to top of `release-checklist-content.md` formatted exactly per the existing cell convention (bold section labels, inline-code variable names, `Variable`= `Value` syntax, `Platform #ID (Name) - Default Value - bool (bit), Enabled - bool (bit)` for tests). Removed all ⚠ TBD markers from the paste block
- 2026-04-27: Round closed. User pasted block into Confluence cell. Section 2.1 non-headline EVs resolved by user decision "leave as on prod" (no owner sanity-check needed; patches non-destructive). Two items deferred: QA-PROD parity alignment (`UseNewUserSearch`, Test 18 Probability) and multi-stream audit on PS/Xbox/MOB/NX before rollout
