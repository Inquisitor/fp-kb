# Backlog — FP-41595

> Umbrella ticket for LBM release-prep work outside other JIRA tasks. Items here are open threads of release readiness; items that grow into a focused investigation should get their own artifact under `artifacts/` (and be referenced from `journal.md`).

## Open

- [ ] **2026-05-08: Validate DB load projection vs reality** — Run queries from [`artifacts/load-projection-2026-04-30.md`](artifacts/load-projection-2026-04-30.md) § Validation protocol on Steam PROD MAIN. Compare actual row counts / sizes / unique-user ratios against projected 1-week values. Refine DAU/WAU/MAU multipliers and Fish diversity factor based on real numbers. Particularly check: Global NC index growth rate, Fish row count vs 7.7-combos-per-user assumption, whether Jobs ran (Status table populated)
- [x] Verify on STEAM PROD (KNW + LBM cherry-picks LBM-001/LBM-010) — done 2026-04-27. Findings: 22/27 EV rows match patch defaults; intentional baseline deviations: `UseNewUserSearch=1` (post-Saltwater activation), A/B tests 16/19 `DefaultValue=true` (feature-on-for-all), test 20 active gold-standard. Expected absences (`UseTrackedDelivery`, tests 21/22, 6 EV→GV moves) — will arrive via LBM patches + GV cherry-pick from QA. `FishStats.CollectFishGenerationStats='Y'` is functionally equivalent to `'1'` (`GetBoolValue` accepts both). To repeat on PS/Xbox/MOB/NX before rollout
- [x] Confirm runtime values for feature flags — done 2026-04-27 from STEAM PROD readout. `IsDailyMissionsOn=N`, `IsLeaderboardsOn=N`, all `Leaderboards.Is*On=N`, `IsRatingByPlaceEnabled=N`, `Connectivity.IsNetworkThreadEnabled=N` — all consistent with patch insert values; flips happen at LBM rollout per release checklist
- [x] Mandatory flip-to-`Y` at LBM rollout — superseded 2026-04-27 by GD requirements: leaderboards stay `N` (fully disabled per GD), only `IsRatingByPlaceEnabled` and `IsDailyMissionsOn` get `Y`. Final values codified in [release-checklist-content.md](artifacts/release-checklist-content.md) → "★ Paste this into Confluence cell"
- [x] After QA→PROD `GlobalVariables` sync: 6 `Leaderboards.*` rows correctness — pre-verified on QA snapshot 2026-04-27 (all 6 in GV with expected values, absent in EV). Final post-rollout verification on each prod stream still pending. Note: GV table is fully replaced by QA sync at release, so verification = "QA had right values when sync ran"
- [x] Build initial release rollout checklist — done 2026-04-27, audit § Rollout checklist (consolidated). Three groups: explicit actions (14 known) / TBD (10 unknowns awaiting owner) / leave-or-verify (7 no-op items)
- [x] Build paste-ready Confluence content for release checklist step "Environment Variables and AB Tests" — done 2026-04-27, [release-checklist-content.md](artifacts/release-checklist-content.md). All rows ⚠ TBD pending owner verify 2026-04-28
- [x] Verify with feature owners — superseded 2026-04-27 by GD requirements (LB off / Rating on / DM on / TrackedDelivery off / new tests inactive returning false). Paste-block in `release-checklist-content.md` ready for Confluence
- [x] Paste paste-block from `release-checklist-content.md` into Confluence release-checklist cell "Environment Variables and AB Tests" — done 2026-04-27 by user
- [x] Section 2.1 "leave as on prod" assumptions — closed 2026-04-27 by user decision: **everything that's currently on prod stays as on prod**. Patches use `IF NOT EXISTS` and never overwrite, so no rollout action needed and no further owner sanity-check required for these EVs (`AccountDeletionImmediateUnbindPlatforms`, `Ping.*`, `Connectivity.*`, `FishStats.*`, `DetailedAnalyticsLoggingMaxLevel`, `UseNewUserSearch`)
- [x] Resolve TBD items in audit § Rollout checklist → Group 2 — superseded 2026-04-27. Headline TBDs (LB/MM/Rating/DM/`UseTrackedDelivery`/Tests 21/22) resolved by GD requirements + paste-block. Non-headline TBDs (`Ping.*`, `Connectivity.*`, `FishStats.*`, `DetailedAnalyticsLoggingMaxLevel`, `AccountDeletionImmediateUnbindPlatforms`) closed by user decision "leave as on prod"

## Deferred (revisit before rollout)

- [ ] (deferred) Align QA-PROD discrepancies for parity: (a) `UseNewUserSearch` (QA=0, PROD=1) — push PROD value to QA if QA needs to test this feature; (b) Test 18 `Probability` (QA=0.5, PROD=0) — pick canonical value with GD/LiveOps
- [ ] (deferred — before rollout) Repeat audit SQL on PS/Xbox/MOB/NX prod streams. STEAM PROD already verified 2026-04-27. MOB/NX (Maldives/IMV20250220) carry larger JLM/KNW/LBM patch span — higher chance of surfacing inconsistencies than KNW-level Steam/PS/Xbox
- [x] Catalog and group "out of immediate focus" EVs/A/B tests by meaning — done 2026-04-27, see audit § Other features — grouped by meaning (Connectivity, Stats/Analytics, Account/Identity, Anti-cheat, Product delivery, Personal Offers A/B tests, FTUE A/B test, one-time data cleanup)
- [x] Resolve `investigate`/TBD desired-prod entries — superseded 2026-04-27 by audit § Rollout checklist → Group 2 (10 consolidated TBDs, more granular)
- [x] Cross-check `AbTests.IsActive`/`Probability`/`DefaultValue` semantics on prod — done 2026-04-27. A/B test runtime: `IsActive=true` → split by `Probability`; `IsActive=false, DefaultValue=true` → feature ON for all; `IsActive=false, DefaultValue=false` → feature OFF for all. Patch defaults are insertion-time only; once on prod, values are product-managed gold standard. Documented in audit § A/B test runtime semantics. Prod current state captured for tests 16/17/18/19/20 — test rows 21/22 will arrive via LBM patches with patch defaults (`Probability=0.5`, both off-for-all on insert)

## Deferred / Notes

- **Scope clarification (2026-04-27):** `GlobalVariables` and `JsonVariables` are out of release-checklist scope. Both tables are fully replaced from QA via DataPump (release checklist step "Transfer data updates from QA"). Patches that touch GV/JV are no-ops on prod (their effects get overwritten by sync). Only EV/AbTests need rollout-action entries. The 6 LBM-002 EV→GV migrated rows are the only narrow case where GV is mentioned — and even there, the GV side comes from QA, not from the patch
- LBM.M.2025.12.17-002 moves 6 `Leaderboards.*` rows from `EnvironmentVariables` to `GlobalVariables`. Effective sequence at rollout: (1) patch DELETEs from EV (effective on prod); (2) patch INSERTs into GV (overwritten by QA sync); (3) QA→PROD GV sync delivers final GV values. Verify EV-side absence and GV-side presence on each prod stream after rollout
- TestId=16 (`Personal Offers A/B Test`) was created via release script `SQL/Releases/R202407-PersonalOffersAB.sql`, not via an `AppliedPatches`-tracked patch — likely already on all prods from earlier releases, but worth a one-time check
