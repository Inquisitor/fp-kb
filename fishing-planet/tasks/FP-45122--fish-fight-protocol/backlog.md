# FP-45122 — Backlog

- [ ] Diagram tooling candidates for D1 (Graphviz vs PlantUML vs Mermaid→SVG); requirement: generator reads the C# transition tables, output embeds into Confluence
- [ ] Draft server answers to the client-deck open questions (contract format, generation semantics, snapshot vs diff, echo-mutation fate, shared stamina model) — input for D3
- [ ] Decide the fate of FP-36584 (client rollback-handler completion, On Hold): superseded by protocol v2 or still needed before it
- [ ] D2 input: pull the authority examples already discussed (fish fight server-interruptible almost always; snagged tackle requires active client effort) into the matrix skeleton; divergence-policy §4 per-row table is a further input — the 2258 letter accepted it "without objections" (it did NOT counter with the matrix; the matrix is our internal D2 deliverable and subsumes the per-row policy at authoring time)
- [x] Write the five named pin tests before D3 closes — DONE: SRV r16427 (all five), r16428 (contract-pin assert strengthening), r16429 (comment revision fix); four Unit pins pass in suite, contract pin verified twice against a live local stand; letter `reviews/2026-08-16-1454-srv-pins-landed.md` (protocol-docs `fefa4a2`)
- [x] TWO ANSWERS GATE the `schema-v2.0` tag — DONE: reply letter `reviews/2026-08-17-1736-srv-envelope-types-and-t0-confirmed.md` PUSHED 2026-08-31 (`a19592c`) with the user's wording fix applied (§3 rule split: v2 norm "echo is never an answer" / as-is descriptor with a declared death date); both answers are "yes" — types confirmed against `ErrorCode.cs` at SRV r16427, `t0` accepted as the window-opening moment; pre-tag checklist of four cheap doc edits handed to the client side as the document's author
- [ ] Format-gate input (user observation, 2026-08-17): the no-unsigned rule's real scope depends on the encoding architecture — it binds only while envelope/message fields travel as individual Photon-typed parameters; under a packed-buffer format (protobuf/flatbuffers/own codegen -> single `byte[]` parameter) the rule collapses to "the blob parameter is byte[]" and inner field types are free. The rule as accepted in the 2026-08-17 letter follows the fields-as-parameters framing of envelope rev.2; mark it conditional at the schema-format decision and surface to the client side with the format discussion. USER POSITION (17.08, input for that decision): a low-level implementation must not constrain the high-level protocol; the Photon packer's only merit is incumbency ("already wired in and working, and even that with limitations") — no known allocation/compactness advantage; leaning packed-buffer. Parameter-level check walked through 2026-08-17 and dissolved: in a pure-blob design the only outside-resident values are Photon's own operation/event codes (Photon's bytes, unproblematic); version check and slot routing move inside the blob decode — an ordering change, not a blocker. GameCarrier byte[] ceilings/behavior still to verify — full GameCarrier source is available on our side (user), so this is a read, not a question to the client. Test-matrix note from the same pass: Xbox forbids unencrypted Photon transport — plain TCP drops out of the Xb column of the 5-installations-x-3-transports verification matrix
- [ ] ANSWER position for 2026-08-31-1911 (walked with the user 2026-09-01, item-by-item): (a) eBp — their ❓ WITHDRAWN (answered directly by the server developer who wrote both halves of the wire — one head, not two independent authors); our server-tree verification goes in as CONFIRMATION: dedicated nullable extractor (`TryExtractBuoyPositionFromRequest`), absence → null ("no point"), present-but-malformed → error log — the "absence != zero" rule lives in that developer's head, not in any written rule, which STRENGTHENS their write-the-rule argument; (b) registry gate — DECLINED for v1 with a stated revival condition: the change burst is Australia-release work ending this week (user's release-calendar knowledge), months of quiet follow, v2 codegen is the gate by construction; the two new keys get §4.3 rows now; shape acknowledged as right FOR v2; no category table exists today — categories go into the 2026-08-19-2100 §4.3 layout as a design doc at the rules pass; (c) the GD throttle verdict (legalize as-is) rides in this letter; (d) `l` row + latch diagnosis (windowed client latch vs phase server stopwatch) + their §4 handler-behavior column accepted with a USER counter-refinement (2026-09-02): TWO orthogonal columns instead of a fourth category — "how absence is interpreted" (zero/previous/declared-unknown) and "side effects of absence" (none/resets slack stopwatch/…); the category derives from the PAIR; v2 note: absence has NO side effects by principle — either illegal (refused receipt, state untouched) or declared-unknown (explicit branch); state changes only via declared actions; (e) §5 coupling line: FP-46028 (close-window default flip) and the `l` phase-events cure are ONE package — either lands without the other and things get worse (their own aborted naive fix proves it); (f) §6 reframe for the GD conversation: the fast-reel slack immunity is an ACCIDENT of the leaks and cannot survive v2 as an accident — GD chooses "feature" (v2 implements it as an explicit rule) or "hole" (escapes get more frequent, hands-on tuning); unlike the throttle, "leave as is" costs work either way; sits on the I1-sequencing sign-off (references recorded today CONTAIN the immunity), but is NOT a quick in-office ask — needs hands-on play; (g) USER OFFER (2026-09-01): the user can play an hour-scale live session with targeted scenarios (fights with fast reeling, deliberate slack, heavy fish) and record it with the client's GameActionRecorder — including editor-against-PROD (leveling + data in one); natural home = their FP-45672 real-fight measurement; offer goes into the reply to 2026-08-31-1911, realization worked out with the client side (the instrument is theirs)
- [ ] Glossary: a shared term for a farm installation (the client side's word is "stand"). The term EXISTS in code: enum `ServerInstallation` (`WebServices/WebHooks/WebHooks/ObjectModel/ServerInstallation.cs`, SRV r16427) with `PlatformHelper.GetServerInstallationByPlatformSource` mapping sibling platforms (Epic→Steam, Win10→XBox, Apple/Android→Mobile). Caveat resolved (user, 2026-08-17): the enum lacks `Nintendo` because WebHooks was built in the Denuvo web-service era, before the Nx farm existed, and Nx has no Denuvo — the member was simply never needed. Micro-techdebt, deliberately not worth time; the glossary entry should note the enum is four-membered for historical reasons. Route through the contracts-repo glossary per its conventions
- [ ] TIME-BOUND, before the FIRST schema tag: freeze mechanics. USER CONSTRAINT (2026-08-18, retro item 5): protocol-docs is a PRIVATE two-person repo by design — pinning the schema canon there deprives the TEAM of the source of truth, so "canon lives in protocol-docs" is off the table for the post-freeze world. Open question for the sign-off, deliberately deferred by the user until the time comes ("we will return to it when the time comes"): where the canon lives after freeze (canon MOVES to the code trees at tag time / a separate team-visible schema repo / other), with protocol-docs keeping history+rationale of the design phase. The anti-drift machinery from the 2026-08-17 sketch survives regardless of canon location: vendored copies carry tag name + content hash; drift caught by machine (unit/EditMode tests recomputing the hash); changes never edit copies; generated code never hand-edited (regen-and-diff check)
- [ ] Parked (conditional, constant cost, no deadline): protected master + MR flow for protocol-docs if a third participant ever joins; per-campaign epics/owners if a second contracts campaign (checkpoint/) actually starts
- [ ] DESIGN DELTA for the envelope (user, 2026-08-19, retro item 7; goes through the sign-off, then a letter): envelope §3.6 currently KEEPS the server ignore budget (made visible via receipts). User position: the count-budget was lag compensation by counter (in-flight messages during the no-receipt era, empirically 1-3 at 200-800 ms RTT) and dies with its cause; v2 replaces it with three separate mechanisms — (1) deterministic staleness (discard Moves until the client demonstrates knowledge of the new state; candidate small schema field: client-side "last seen srvSeq" reverse-ack, making staleness exact instead of opcode-heuristic — this is state-machine synchronization taken to the wire), (2) time-based escalation to StateCorrection via the unsync window (already designed), (3) transport-level rate limiting as the legitimate anti-flood remainder (client 150 ms window stays in this category)
- [ ] PENDING USER SIGN-OFF: the campaign invariant as an explicit acceptance criterion — user's formulation (2026-08-19): "change the protocol so that delays and delivery failures cannot BY CONSTRUCTION break the synchronization of the state machines". Exists nowhere in the exchange docs in this distilled form; candidate placement: DECISIONS.md or the schema preamble, so every schema rule is checked against it. User authors/approves the wording before it goes anywhere
- [ ] Envelope §3.6 refinements for the rules pass (user + assistant, 2026-08-19): (a) correct suppression semantics is ACCUMULATE, not drop — force peaks of a suppressed message must merge into the next window (today they are wiped, C-9/C-10 family); naturally solved by t0/t1 windows covering the full interval; (b) §3.6's "every suppressed message gets a receipt" cannot hold for CLIENT-side suppression (a message that never reached the wire gets no wire receipt) — the rule must distinguish server suppression (wire receipt) from client suppression (local recorder/log entry)
- [ ] PROPOSED retro continuation (assistant offer, 2026-08-19, awaiting user's go): full inventory of envelope/protocol RULES — each with what it says, who proposed it, what it rests on, and construction-level vs cosmetic per the user's invariant filter; user reads only the construction-level ones. The pre-tag reading that beats proofreading everything. SECOND FILTER for the same pass (user, 2026-08-23, retro item 15: "no accidents in the contract, no surprises"): no v2 rule may REST on an as-is observation — every schema rule must be derivable from design intent; behaviors that exist only as observed as-is facts may enter solely as bridge contracts with a declared death date (the Clear-contract is the model: as-is distinguisher pinned until v2, v2 norm accident-free)
- [ ] DESIGN DELTA (user, 2026-08-22, retro item 11): envelope §4's anti-cheat paragraph overclaims — "the invariant checks are already written; with dt they only get more precise" ignores that the detection system itself produced the astronomical scores (81/100 top players above threshold => checks fire constantly on legitimate play). User position: existing checks get minimum serious rework, maximum discard-and-rewrite; NO check migrates to v2 without a per-check audit (false-positive rate, threshold provenance, what it actually measures). Known-broken already in the D1 register: D-11 (landing-net check dead), D-13 (catch without validation), P-23 (desync window fed by raw client PingTime). Surface to the client side at the rules pass / v2 anti-cheat design
- [ ] Direct `FinishAttack` coverage for the contract pin — the client fix rides `tP` on `FinishAttack`, the pin samples `Throw`; a per-handler-Clear refactor is the named blind spot (stated in the letter as a follow-up candidate). NARROWED by code walk (2026-08-23, retro item 15): the Clear is CENTRAL — one line in `DoAfterTransition` covering all sixteen client transitions in the dispatch; there is no per-handler Clear today. `tP` (`TerminalTacklePosition`) is read pre-Clear (`ExtractParamsFromRequest`) and NEVER written back by any handler (`HandleAttackFinished` repopulates only server keys: fish JSON, wear/breaks family, stamina). So the Throw pin protects FinishAttack transitively against central-Clear removal; the residual uncovered regression shape is narrow — a change that keeps the central Clear but starts re-echoing `tP` specifically on the FinishAttack path. A direct FinishAttack assert is cheap insurance against exactly that
- [ ] Integrated test category is broken environment-wide (2014 ut profile, missing `UnitTestTemplate` checkpoint in every local DB, bit-rotted harnesses) — candidate separate ticket; the contract pin shows the temp-player repair pattern
- [ ] Deferred minor: defensive assert on the starter rod in the contract pin (a missing/unmovable starter rod currently surfaces as an unhandled exception, not a clean assert)
- [x] Small SVN batch on NPN — DONE: SRV r16478 (2026-08-31), committed against r16477 after a foreign-merge recheck (build green, Unit 307/0/3). Deleted the dead `UpdateObjectModel.cmd`, mirrored `CharacterEventType` to the client numbering, `TryParse` + `IsDefined` fallback with explicit `Initial` in the restore path (the FP-45122 tag deliberately dropped from the code comment — permanent hardening, not scaffolding; provenance via blame). Measurement closing the 2258/envelope-§2.1 caveat (2026-08-23, user-confirmed read-only run): `Fishing.PreserveFishingState = N` on ALL FIVE F2P PROD Mains (Steam, PS, XB, MOB, NX) — the "other platforms are an assumption" rider is retired (it was the user's own decision, now verified); worth a one-line mention in the next letter to the client side
- [ ] GD decision on the backlash data layer: `Reel.BacklashProbability` is authored per reel in item configuration and lives on both sides; decide its fate together with the FSM demolition rather than after
- [ ] Close the numeric `Enum.Parse` hole in `GameActionAdapter` as part of v2 (a whitelist or `IsDefined`, not just removing members) — byte 25 currently resolves to `Transitions.LoseItem`
- [ ] Re-pin the letters to a current server revision before D3 is acted on, or state the pin age explicitly: they were written against SRV r16404 and the tree is already at r16422. Same aging applies to GLOSSARY.md rule 3 pins (the "In code" pins: SRV r16404 / CLN r56602) — re-pin and re-run `tools/check-glossary.py --code-root` in the same pass
- [ ] Strong-fish escape threshold: the code default is 0.165 but `UgcOld/2016.02.08-063` seeds `Fishing.LimitForce = .2`, `CLZ.M.2023.06.13-047` copies that row (its own 0.165 survives only as an `ISNULL` fallback) and `-049` renames it. Worth measuring per platform and telling GD - the `UgcOld` layer is not applied by the patch runner, so the seed is not proof on its own
- [ ] RETRACTED, do not carry forward: "the lure-fish step counter lost its 8 in 2024". `GRM.M.2024.08.19-030` deletes from `GlobalVariables` while the accessors read `EnvironmentVariables` - different stores, so 6/6 applied before the patch as well and nothing changed. The 8 lives only in the `FishSelector` field initializer, overwritten at startup

- [ ] Method note for future audits: values of GlobalVariables/EnvironmentVariables are readable from `SQL/Patches` in the branch - fifteen review rounds searched only the C# and repeatedly concluded "this needs a database measurement" for values the repository already carried
- [ ] Make the RNG seed settable in five server generators for I1 reference recording (client request): `GameProcessor`, `StrongFishEscapeModel`, `FishGenerator`, `FishTireModel`, BiteSystem `PlayerData`. Two already expose `RngSeed` and need only a constructor parameter; `HitchGenerator` takes a `Random` already. Split `GameProcessor.rnd` into per-purpose streams while doing it - hitch, breaks, bite and fish count share one today, so any change to call counts shifts the others. USER FRAMING at retro verdict (2026-08-23, item 16), use it in the future ticket wording: client randomness is INPUT to the server (same standing as player input, different origin) — the server is a deterministic function of (recorded stream, seeds); I1 freezes both arguments. Fold in the letter's §1c: seed written into the fight log makes any player's fight replayable (bug reports, disputes). GD-facing rider: client-fight non-reproducibility accepted as a product property — liveliness tunable via GD options. ORDERING + COMPARISON MODES (user question at retro, 2026-08-24): the stream split MUST land before any reference recording (splitting is distribution-preserving — same rolls, same probabilities, different realized numbers — so it is safe on the old branch, not a silent rebalance; a post-recording backport would just invalidate the corpus). Two comparison modes: exact replay (seed+stream → identical outcomes) verifies distribution-preserving changes (envelope, codegen, transport); the 3.2 per-duration conversions change roll counts BY DESIGN and are verified distributionally — `1−(1−p)^dt` is the probability-preserving map, checked by seeded-run outcome statistics, never draw-by-draw. SCOPE NARROWED at retro item 17 verdict (user, 2026-08-24): bite/generation touch the protocol only through the client's end-to-end-replay methodology, not through the protocol itself — I1 required scope = the four 22.07 positions + `FishGenerator` seeded for `GenerateEscape` only; fight entry via ASSIGNMENT, not generation replay — CORRECTED at the 2026-09-01-2024 letter review (adversarial pass caught the earlier conflation): the scripted-fish paths DON'T fit (`PondScriptedFish` = probabilistic marketing event with randomized selection AND weight; the lure guarantee = one-shot FTUE starter with hardcoded species); the REAL assignment mechanisms, both live: `SysSubOperationCode.SpawnFish` (`IsDevServer`-gated → `SetFishToGenerate`: exact species, weight, hooking result) and per-profile `DebugConfig` (`DebugFishId`/`DebugFishWeight` + `DebugImmediateFish`/`DebugAlwaysHook`/`DebugNoEscape`, per-slot, delivered via the existing chat request) — the client's yellow-tuna-2kg measurement used one of these (doc doesn't name which; both fit). `BiteSystem.PlayerData` seed demoted to an optional rider of the §1c replay-any-fight idea, outside the campaign's critical path; tell the client side when answering their letter
- [ ] Guard the `TODO: refactor to Random.Shared` in `NormalRandom` - `Random.Shared` cannot be seeded, so executing that TODO would undo the reproducibility work. Consider whether `System.Random` is acceptable at all if references must outlive a runtime move, since the algorithm changed in .NET Core. USER (2026-09-02, after the xorshift128+/splitmix64 sketch): the stable RNG is wanted — "we need a tool like this", also a candidate for the unit-test rigging, not only the fight generators
- [ ] Strike-by-reeling roll — USER POSITION SET (2026-09-02, annotation on 2026-09-01-2024): NOT left to the client — "a ready-made exploit"; in v2 the roll moves to the server; stated in the letter, formal closure at the authority matrix. (Was: decide with GD; today `IsReeling && Random.value > 0.5` on the client, arrives as `iS` unchecked)
- [x] GD DECISION on the dead strong-fish escape throttle — RESOLVED 2026-09-01, GD lead, quick in-office ask: **legalize as-is** ("relax it later if need be"): the per-message roll (~5/s) is declared the intended behavior; the never-lived 1/s design is abandoned. Consequences: (a) the I1 reference-recording gate is OPEN — no re-recording risk from this item; (b) PING owed to the client side (their 1830 §4 rider) — carry it in the Slack reply and/or the seeds letter, where the question was raised; (c) the `StrongFishEscape..._KNOWN_DEFECT` pin changes meaning: it now freezes LEGALIZED behavior, not a defect — rename/re-comment at I1-I2; (d) the 3.2 conversion target for this mechanic = reproduce the LEGALIZED per-message outcome distribution via `1−(1−p)^dt`, not the 1/s design; the dead throttle fields (`lastEscapeCheckTime`, `EscapeCheckTimeout`) become removable dead code in the v2 rework; (e) "relaxing" later = a conscious rebalance, post-references it forces re-recording — future GD call under the same rules
- [x] ANSWER OWED to schema rev.3 §6 — DONE: `reviews/2026-08-30-2241-srv-sticky-keys-intent.md` PUSHED 2026-08-31 (`13eb40e`) after a two-reviewer adversarial pass (Codex 7 findings + agent 9, one real BLOCKER: the distance-consumer map — recomputed distance feeds ONLY the fish-generation gates, all Move-only; the fight-cycle harm of stale `tP` is `hookDistance` at strike → XP + catch record; a convention-10 correction block to our own earlier "tP -> anti-cheat" claim went into the letter). Classification as presented to and accepted by the user (below, kept for reference). Original ask: for each STICKY key (no-else `ContainsKey` read into a processor member — absence means "value of the previous message"; their list from our own deck-proofread confirmation: `pP`, `tP`, `sO`, `eAw`, `b` in `GameProcessor`; `pP`, `pR`, `bS`, `tR` in `MultiRodGameProcessor`; lower bound) — is there a handler for which the stale value is INTENTIONAL behavior? Intentional ones become explicit contract; the rest are cleaned in v2. Their measurement: `tP` absent in 10 of 15 opcodes; benign where the object is still (Walk, TravelByBoat), bites in `FightFish` (tP came 1 of 7 — feeds `distanceToTackle`, `tackleStatus` → hitch generator, lure attraction, wear; small sample, their FP-45672 real-fight recording pending). v2 rule: presence categories per (key × opcode), no sticky fields; the envelope does NOT close this (t0/t1 date the window, not the field). Feeds the rules pass. ANSWER DRAFTED from the NPN r16456 code walk (2026-08-28, presented to the user): (a) INTENTIONAL, event-carried state/toggles — `sO` and `eAw` (dedicated client senders only, message = change notification, member = current state), `tR` (trolling mode, feeds MissionsContext), and `b`/boat type (CYCLE-scoped: explicit `usingBoatType = null` reset on Throw + `transitionData.Remove(AnyBreaks)` after read to dodge the response collision) — v2 models these as state with a named lifetime, not per-message optional samples; (b) ACCIDENTAL, per-message samples — `pP`, `tP` (GameProcessor), `pP`, `pR` (MultiRod): no designed reset or lifetime, distance recomputed from possibly mixed-age halves once both members are non-null — v2: mandatory per opcode where meaningful, else explicit-unknown; (c) `bS` (boat stamina) — client-owned reported value the server cannot simulate: persistence is the only possible behavior but absence semantics were never designed. Supporting observation: the same extract methods else-reset most keys (forces→0, `fishId`→null; MultiRod's own `BoatType`→null) — the sticky ones are exceptions, and the codebase already distinguishes state from samples, just not uniformly
- [ ] D2 authority-matrix input + anti-cheat-audit entry (user question at retro follow-up, 2026-08-30): `bS` (boat/rowing stamina) is CLIENT-AUTHORITATIVE with one dead tripwire — the server accepts it verbatim (`BoatManager.Travel`: `stamina = boatStamina`, persisted, no plausibility model), feeds missions via `IsBoatStaminaFull`, and the only check (`_staminaGrowOnRowingLoger`: fires on `wasRowing && stamina > 0 && deltaS >= 0`) reports into the demolished scoring; CORRECTED at Codex review 2026-08-30: a client naively holding stamina=1 DOES trip that check (deltaS=0 satisfies >= 0) — into dead scoring only; a client reporting FAKE DECAY trips nothing (the server has no decay-rate model); the velocity cap check compares against boat max, not tired-speed, in both cases; mission conditions on full stamina are trivially fakeable. v2 fates to decide with GD at the matrix: (a) client cosmetic — take missions off it; (b) server stamina model — near-free once 3.2 duration mechanics land (decay/regen as f(dt, wasRowing)); (c) plausibility corridor protocol-level DELTA COMPRESSION — a presence bitmask declaring "field unchanged, use previous" so the wire saves bytes while HANDLERS always receive the full form (codec reconstructs; staleness never visible to logic). Key distinction vs the as-is disease: as-is stickiness is ambient and indistinguishable, the mask makes it explicit and verifiable. Notes: protobuf/flatbuffers do NOT do cross-message delta (protobuf3 absence = type default, not previous; flatbuffers optional = absent) — delta encoding is a layer ABOVE serialization and needs an ACKED baseline (delta vs last-acked state, Quake-style), which is exactly what v2's ackSeq/srvSeq machinery provides — the idea composes with v2 counters rather than fighting them. For v2: only make sure the schema layout does not PRECLUDE a presence mask later; measure real savings before committing (user: "this still needs measuring"). Target: v3
- [ ] lTf ceiling: the heavy-fight live measurement (2026-08-24) shows max exactly 40.000 — a hard ceiling. Identify the clamp and whether it distorts wear/unhitch inputs; fold into the GD thresholds package
- [ ] Answer the client's open question: `Throw` carried its full key set in only 7 of 11 measured messages
- [ ] Ask the client which revision and file shows 35 members in the `SERVER_FSM_LOGGING` mirror - our pinned tree (CLN r56602) shows 33, missing `ElectricAutoWinding` and `Reset`
- [ ] Confirm the wear impact of their `FinishAttack` zero-force finding: `UpdateWearSystem` takes forces from the same message, so wear on `FinishAttack` is computed from zeros in one of two frame-timing branches
- [ ] I2/I3 demolition: the five FP45122 pin tests are scaffolding, each dies with its fix — StartDraw with the Draw-family whitelist removal, JerkUnhitch with the r1435 guard, StrongFishEscape with the throttle fix or model rework, Rollback with the in-method Header guard (an upstream-normalization fix keeps it green — delete by hand per its comment), TransitionDataClear with the v2 envelope receipt. Grep `FP45122` in LoadBalancing.Tests and remove/replace; none of them may survive into v2
- [ ] Rules-pass correction candidate (user annotations on divergence-policy, 2026-08-22, retro item 12): §2C's "no separate notification is needed for this class" holds only under continuous client traffic — an idle rod produces no receipts, so the "not later than the next tick" bound fails (their own §2D admits part of legal fights look silent today); and state-field diffing alone is guessing — the designed mechanism is srvSeq gap detection plus the event channel with its own srvSeq (our 2208 §3 already refutes the general form: async server transitions leave as events). Surface at the rules pass together with the §3.6 refinements
- [ ] Rules-pass design question (user, 2026-08-22, retro item 12): response-vs-events ordering. User recalls a server mechanism pausing event sends until the operation response goes out (client sees the answer first, then the events its action spawned) — verify in code whether it exists/existed before designing around it. Candidate design answer: treat the event channel as a srvSeq-ordered transition log and the receipt as a checkpoint carrying position-after-processing — then arrival order stops being load-bearing (client applies events in srvSeq order; the receipt says "position N, state X"; a missing k<N is a detectable gap), and response-first ordering becomes an optimization, not a correctness requirement. Open sub-question: receipts carry final state + applied-transition count, not the transition PATH — decide at the rules pass whether the divergence detector needs the path for nested (non-event) transitions
- [ ] Server-side position on the player signal, input for the GD conversation (user as server developer, 2026-08-22): netcode exists to smooth over physical-world network trouble; interrupting play with a message ("your strike lagged behind the server") is a last resort; conflicts should be closed by game mechanics, continuously and smoothly — GD is expected to answer the same. The real answering happens when each conflict class gets its concrete resolution; error handling here is core-mechanic-scale work and cannot be ignored. Extends 2258 §5 (capability promised, decision GD's) with the server-side default: smooth-first
- [ ] D2 CORNERSTONE INPUT, recovered from pre-campaign history (the user's matrix work with a server developer, resurfaced 2026-08-23): FIXATION POINTS — designated transitions are executed SYNCHRONIZED: the server may decide the transition is due but does not advance until it drags the client through it. Consequences: (a) the authority matrix needs no rows for state pairs separated by a fixation point — "server casting / client landing a caught fish" becomes impossible by construction (direct realization of the campaign invariant); (b) class-A legal windows, INCLUDING the client-ahead direction, exist only BETWEEN fixation points; (c) CatchFish is the model fixation point (see the catch-flow item). Feed into the D2 matrix skeleton. GLOSSARY LINK (found 2026-08-23, retro item 17): the term ALREADY EXISTS — the commit-point entry, status candidate, introduced by the srv 2140 state-map reply §5 ("an irreversible point of the graph where value is created or destroyed: fish generation, strike, catch, break, eaten bait; a domain boundary, not a graph one"); the latch entry already carries the v2 rule "never commit the irreversible beyond a confirmed commit point". The user's synchronization rider (transitions THROUGH a commit point execute synchronized; no matrix rows across one) is the NEW half — write it into the entry/policy at the rules pass
- [ ] DESIGN DELTA, rules-pass input (user, 2026-08-23, retro item 14, annotation on envelope §3.1): the GAP-STALL attack — a compromised client declares `gap` (or its degenerate form: simply stops sending) to SLOW time, holding a fish indefinitely: no dt credited → no fatigue, no break progress, no escape rolls. Analysis: the dt formula CANNOT fight this by construction — the upper bound is enforceable (server clock refutes overclaim), the lower bound is not (silence is always available); the same stall already exists in as-is trivially. Anti-stall instruments are therefore OUTSIDE the interval math: (a) the class-D fight deadline running on SERVER wall-clock, indifferent to declared gaps — "envelope first, then the deadline" already sequences it; the deadline must never be dt-based; (b) per-reason gap policy with GD: how long may "minimized"/"connection lost" stall a LIVE fight before a forced outcome; (c) gap telemetry — declared stalls become countable per player. v2 does not create the stall, it makes it declared and boundable. USER GOAL FORMULATION at verdict: "do not let the client stretch the fight to its own benefit" — the benefit test scopes the countermeasures (a stall with no gameplay benefit is not the target)
- [x] Wording-fix candidate for the parked types+t0 letter (user annotation, 2026-08-23) — APPLIED before the 2026-08-31 push: the §3 rule now reads "a request echo is never a response; a suppressed message gets an ignored receipt (3.4/3.6)", with the echo-distinguisher demoted to an as-is descriptor with a declared death date
- [ ] Schema/D2 input (user, 2026-08-23, retro item 12): v2 catch flow as the model fixation point — the server first PERMITS the catch-show (guaranteeing the fish is beaten and cannot escape), the client reports starting the show, and value fixation (XP, money, final weight) arrives only as the server's message. Information-disclosure rule for the schema: final weight and derived rewards must NOT be disclosed before the fish is landed — the take/release decision is made only at landing, and licenses, fines, and the kukan rule (fish wounded by the stringer cannot be released) depend on it. The schema must model WHEN value information becomes visible, not only who fixes it
