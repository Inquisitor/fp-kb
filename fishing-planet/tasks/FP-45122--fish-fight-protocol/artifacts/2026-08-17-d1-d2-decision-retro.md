# D1-D2 Decision Retro — re-owning the decisions made at client tempo

Started 2026-08-17 at the user's request: D1/D2 passed him "in fragments, with instant opaque
decisions" — this retro walks every decision one by one so each is consciously re-owned,
reopened, or annotated. Verdicts: HOLDS / REOPEN / DISCUSS. Format per item: what was decided,
how it passed, what it binds, reversibility, assessment.

| # | Decision | Origin | Verdict | Notes |
|---|----------|--------|---------|-------|
| 1 | One release, forced update, no compatibility, no flags | epic, 22.07, user-authored | **HOLDS** | The "cert windows must align" tail turned out to be a non-issue: five independent farm installations (PC/PS/Xbox-UWP/Mobile/Nintendo), each updates on its own schedule, cross-play only within an installation. FFPv2 rolls out per-installation whenever each farm releases it — Mobile/Nx still on IMV proves the lag model is normal. |
| 2 | Understand-first: plan restructured from contract-first (S0-S6) to D1-D3 -> I1-I3 | 22.07; S0-S6 was the ASSISTANT'S draft, the USER stopped it | **HOLDS** | Attribution corrected during retro: the user stopped the assistant's codify-fast instinct, not a client plan. Retrospectively paid for itself — the seventeen review rounds caught dozens of factual errors on D-material that contract-first would have frozen into the schema. |
| 3 | Shadow fields cancelled: wire untouched until the v2 flip; pre-flip measurements via server logging; client receiver built straight against v2 | 05.08, both sides aligned | **HOLDS** | Fuller story recorded during retro. The shadow proposal was reasoned from a compatibility premise that does not hold under the five-farm release model (each installation flips client+server together; cross-installation compatibility is never required). Parallel branches also measure better than transitional shadow code: the old branch keeps unmodified code, while shadow machinery would risk distorting the very behavior being measured; `IsDetailedLogging` can be enabled per environment when a measurement is needed. Once the release model was on the table, both sides converged on clean v2. Measurement caveat: the `HandleUnhitch` observation-logging promise remains an explicit I1/I2 debt. Standing rule (convention 12, generalized): an authority label on an exchange position is not evidence — weigh the reasoning; when a push is insistent but its premise is invisible, ask for the premise before aligning. Positive anchor for weighing proposals: the campaign's two cornerstone requirements — synchronized state machines and per-message receipts — are long-standing owner requirements predating this campaign; incoming proposals are checked against THEM, not against who signed. |
| 4 | Schema as the single source of truth: generated DTOs, binary serialization, schema-generated readable debug log on both sides | 22.07, user co-authored; client constraints arrived 05.08 | **HOLDS** (deep-dived via 27 user annotations on the client input doc) | Constraint reality confirmed: IL2CPP/AOT means build-time codegen only (no Reflection.Emit, ELI5 delivered down to generic instantiation boundaries); "five stands" = our five farm installations seen from the client shore (glossary candidate: live enum `ServerInstallation` in WebHooks; no Nintendo member — Denuvo-era artifact, deliberate micro-techdebt); ITransport layering was the user's own requirement originally. TWO format-decision inputs extracted: (a) client's AOT specifics must arrive as a concrete failure list, not slogans; (b) USER: the no-unsigned rule is parameter-level only — under a packed-buffer format it collapses to "blob is byte[]"; low-level implementation must not constrain the high-level protocol; Photon packer's only merit is incumbency. Xbox forbids unencrypted Photon transport (test-matrix note). User requirement added: the protocol interpreter (debug pretty-printer) must NOT ship compiled into the release client — cheat-surface; server-side readable exchange log wanted, toggleable. Format choice goes through a full sign-off. |
| 5 | Contracts repository: separate git, append-only reviews/, campaign-per-folder frame, tags as freeze points | 06.08, server-side proposals, owner approved | **HOLDS** | The repo is PRIVATE two-person by design (user+owner); third-party access is a parked conditional (protected master is trivial whenever needed), checkpoint campaigns get their own epics when real. The load-bearing discovery: "canon lives in protocol-docs" is OFF THE TABLE post-freeze — a private correspondence repo cannot be the team's source of truth for the schema. Canon location after the first tag = open question, deliberately deferred by the user; the anti-drift machinery (tag+hash headers, machine-checked copies, no hand edits of copies or generated code) survives regardless. Time-bound: settle before `fish-fight/schema-v2.0`. |
| 6 | Measurement split: server measures OUTCOMES on live players, client measures INPUT QUALITY on an instrumented build | 06.08, proposed by the client (reviews/2026-08-06-1456), accepted same hour (1509); registered DECISIONS.md:33 | **HOLDS** | Re-lived with sources open. Primary subject of that exchange was the slack flag ("l" lives in combat: heavy-fish escapes via `lineSlackStartDate`/`SlackEscapeDelay`, both leader cutters, missions mirror, telemetry) — the split was the measurement plan for the GD rebalance decision it triggered ("the leaks shield fast reeling"; no silent fix). The split principle is sound: wire cannot distinguish "false" from "short packet", so a server-side flag-quality metric would be noise. Riders recorded, decision unchanged: (a) the SERVER half of the measurements is an open debt with a natural deadline — before the unhitch GD decision (`HandleUnhitch` observation logging + outcome stats); (b) sequencing "I1 references vs C-7 leak fix vs GD" is a future sign-off — references recorded on today's leaky flag would bake the leaks into the "feel baseline"; (c) live-outcome stats will come from one installation first — extrapolation to other farms is an assumption, mark it per their convention 2. |
| 7 | Receipt for EVERY game message — a requirement, not a proposal | 10.08, owner decision — one of his two AUTHENTIC 12-year requirements | **HOLDS** | The rare item with nothing even conditional to flag on the core: the campaign's whole defect diagnosis (P-1 no-response paths, P-2 duplicate-success echo, silent swallow budgets) vindicates the 12-year-old demand. Server cost moderate (Photon already answers every operation; the receipt rides it); the real new cost is the EVENT channel (srvSeq + gap handling on broadcasts). Justification honestly evolved: "otherwise unfixable" was retracted by the client, the standing form is "otherwise it rests on chance" — now pinned. The deep-dive spawned three recorded outputs: the ignore-budget design delta (counters die with their cause; staleness/escalation/rate-limit instead + reverse-ack candidate field), §3.6 suppression refinements (accumulate-not-drop; server vs client suppression), and the campaign INVARIANT formulation awaiting the user's visa. |
| 8 | State machines: for verification and anti-cheat, NOT unification; client builds a divergence detector on confirmed server state | 10.08, owner decision; realizes his authentic "synchronized machines" requirement via detector+receipts rather than shared code | **HOLDS** | Server answered the enabling questions in 2208: srvSeq accepted (increment at applied transition; EventId not reused — per-peer, non-atomic, burns on failed send, resets on reconnect); the server's own strengthening: async server transitions leave as EVENTS, so the event channel needs its own srvSeq + gap handling; honesty limit — srvSeq monotonic PER CONNECTION only (PreserveFishingState off; FP-45678). USER RIDER (22.08, annotation on §4 lines 248-250, "physics on the server / the message suffices for reproduction"): goal supported, achievability doubted — and the doubt is well-founded by the client's own I1 reasoning (frame-tied physics, ~55 client generators → true re-simulation impossible without deterministic fixed-timestep physics, a separate large project). Practical reading: the requirement is cheap as a design posture (fields must not destroy information needed for verification — no collapsing forces into booleans), and only that much is promised ("owner: maybe later"). |
| 9 | Client lTf fix is not reverted and ships with v2; server takes three commitments (thresholds to `GlobalVariables`, `HandleUnhitch` observation logging, unhitch pin under I1) | 10.08, both sides, same-evening exchange | **HOLDS** | The model pattern of the campaign: live defect found -> not silently fixed -> conserved to v2 -> pinned -> calibration goes to GD with numbers promised. Of the three commitments one is delivered (the pin, r16427); thresholds-to-GlobalVariables and observation logging are open debts sharing one natural deadline (GD before I1 reference recording) — likely ONE work package ("server observability of unhitch/escapes"), candidate single ticket at planning time. User note at verdict: GD's timing is not ours to promise; our debts enter the queue by priority once the retro is done and our own tempo is restored. |
| 10 | `NeedClientReset` -> `StateCorrection`: enum target, mandatory else branch, acknowledgement; stays ONE message | 12.08, D2 five-letter volley | **HOLDS** (user: "this one I remember") | Repairs the only forced-sync mechanism, which today has never helped anyone: C-6 (payload decoded and discarded, no default branch, pod-rod Initial ignored), plus the client-found fourth defect that removed our own premise — "Initial resets the rod" holds only for the ACTIVE rod, and the flag is cleared in onEnter before anything reads it (server resets mostly touch inactive slots; teleport resets in a volley) — hence the client half is "built, not completed". Construction-level open point, honestly owned by the client side: the full catalogue of correction TARGETS (which states the server may impose and what the client must do per target) is not yet specified — goes into the pre-tag rules pass. Ties: acknowledgement rides the receipt channel (item 7); enum targets ride the shared vocabulary (§6 item 5); the reverse-ack candidate strengthens this too. |
| 11 | Anti-cheat SCORING/auto-ban demolished whole, not repaired | 11-12.08; measured: 81 of top-100 above ban threshold, mean 224572 vs threshold 20, `BanCheaters` off everywhere | **HOLDS** (scoring) + **USER RIDER reopens the checks half** | User: consciously decided back then ("wanted to rip the anti-cheat out for a long time"; he deliberately skipped even reading the dead-checks letter — informed, not opaque). His inversion at retro (22.08): the astronomical scores were MADE by the detection system — the measurement indicts the DETECTORS, not just the scoring; the checks must not be taken seriously as-is: minimum serious rework, maximum discard-and-rewrite. Counterweight noted (huge scores could also be low-rate x years-of-exposure without decay), operationally irrelevant: NO existing check migrates to v2 without a per-check audit (false-positive rate, threshold provenance). Consequence: envelope §4's "the invariant checks are already written; with dt they only get more precise" is an overclaim (client-authored, relayed uncritically by the server assistant in this very retro) — correction candidate for the rules pass; the D1 register already lists broken checks (D-11 dead landing-net, D-13 no-validation catch, P-23 ping-fed window). |
| 12 | Divergence policy accepted as D2 basis: classes A–E with per-class policy, the B0–B2 ladder, the B1 value clause, per-row §4; class A generalized to a rule (any server-initiated transition opens a legal window) | 11.08 client doc + srv 2258 answer | **HOLDS** | Walked 2026-08-22/23 via 13 user annotations with the source open. Riders born here: (a) §2C's "no separate notification needed" holds only under continuous client traffic — srvSeq gap detection is the mechanism, state-diffing alone is guessing (rules-pass correction, backlog); (b) response-vs-events ordering question + "event channel as srvSeq-ordered log, receipt as checkpoint" candidate answer (backlog); (c) the class-A client-ahead direction narrows in v2: FIXATION POINTS recovered from pre-campaign history (the user with a server developer) — synchronized transitions, no matrix rows across a fixation point, CatchFish as the model case with the value-disclosure rule (weight hidden until the fish is landed); (d) player-signal server position recorded: smooth-first, interruption is a last resort (backlog, GD input). Backlash/Draw removal re-fixed by the user at verdict ("noting the demolition for myself"). |
| 13 | No-unsigned rule for the envelope, declared PERMANENT on the strength of the 12.08 "owner decision" that v2 lives on both encodings (Photon + GameCarrier) its whole life | 13.08 client measurement + rev.2 §2.1; the premise relayed under the owner label | **HOLDS operationally, premise downgraded** | The rule's factual base is measured and solid (seven of fourteen rev.1 fields unsendable via Photon; `reason` → `ErrorCode : short` with negative values; range consequences consciously accepted). User re-affirms the item-4 position: the rule is PARAMETER-LEVEL only — under a packed `byte[]` format all inner field types are free, so the rule stays CONDITIONAL on the format sign-off (backlog). The "both encodings forever" premise gets the convention-12 treatment (an authority label is not evidence — weigh the reasoning): the user does not take it as a considered, binding owner requirement; it is treated as an acknowledgement of the dual-transport reality, nothing more, and the real decision point remains the format sign-off (GameCarrier `byte[]` ceilings read + sign-off already queued). |
| 14 | `t0` defined as the moment the accumulation window OPENED, not "time of the previous message"; gap intervals never credited to `dtUsed` | 13.08 client rev.2 §3.3 precise definition; server agreement drafted 17.08 (letter reviewed by the user, parked) | **HOLDS** | The one D-phase decision the user had already participated in (letter review); retro was verification, not re-owning. The adversarial case confirmed: under the naive reading an idle slot's first `Throw` claims minutes and 3.1's `min()` does NOT cut it (server clock agrees) — the window definition closes the hole by construction and covers the C-9/C-10 suppression-wipe family. Riders born at verdict: (a) GAP-STALL design delta (user): the dt lower bound is unenforceable by construction (silence is always available) — anti-stall lives OUTSIDE interval math: wall-clock fight deadline (never dt-based), per-reason gap policy with GD, gap telemetry; user's goal formulation: "do not let the client stretch the fight to its own benefit" (backlog); (b) the two client window leaks walked in code (`Game.cs`: short `Move` overload sends without `ResetPeriod()`; long overload's `ResetPeriod()` sits outside the `IsMovingToRoom` guard) — fixed in the v2 package, not before (silent fix = rebalance); (c) wording-fix candidate for the parked letter's §3 rule: split the v2 norm (echo is never an answer) from the as-is echo-distinguisher descriptor (backlog); (d) user 👍 on rule 3.4 (receipt always). |
| 15 | `transitionData.Clear()` elevated from implementation accident to CONTRACT: applied → server-keys-only rebuilt response, swallowed → untouched echo; scope bounded (PerformTransition/DoAfterTransition path only), three response forms named (rebuilt / echo / no-data-block for refused) | 11.08, srv 2208 §6, answering the client's §3.4 ❓ | **HOLDS** | The most re-lived item of the D phase: user authorized the pins, the contract pin landed (r16427/28, verified live twice), and he himself reshaped the schema formulation (v2 norm split from as-is descriptor). At verdict: (a) user challenge "no accidents in the contract" resolved by the two-layer reading — the DECLARATION kills the accident for the bridge period, the accident-SHAPED content (echo = swallowed) lives only as a bridge contract with a declared death date, the v2 norm is accident-free; generalized into the SECOND rules-pass filter: no v2 rule may rest on an as-is observation (backlog); (b) FinishAttack blind spot narrowed by code walk: Clear is central (one line, all sixteen transitions), `tP` never re-echoed by any handler — Throw pin protects FinishAttack transitively, direct assert = cheap insurance against one narrow regression shape (backlog updated); (c) user re-confirmed at the 2208 window: the echo form and the "swallowed" category both die in v2 (3.4+3.6), all three forms collapse into the uniform receipt. |
| 16 | I1 freeze methodology: reference = the recorded OUTGOING message stream (input replay proven impossible: ~55 client RNG sites, frame-rate-dependent content and count); server precondition = five seeded generators + per-purpose streams + own stable RNG + `Random.Shared` guard | 13.08 client letter (1900), addressed to the user personally; no reply letter — server intake was operational (backlog + the throttle KNOWN_DEFECT pin) | **HOLDS** | User's framing at verdict, cleaner than the letter's own: CLIENT RANDOMNESS IS INPUT to the server — same standing as player input, different origin; the server is a deterministic function of (stream, seeds), I1 freezes both arguments and compares outputs exactly; "hard to compare" collapses onto the feel half only (item 6 split), and client-fight non-reproducibility is accepted as a product property ("livelier"), tunable via GD options — GD-facing rider recorded. Assistant counterweight kept in the row: the stream is a moving target (physics fix r56997 already landed; window leaks, FinishAttack zero-forces, 150ms rework, throttle fix ahead) — the item-6 sequencing sign-off grows into an explicit dependency plan (GD decisions → server observability → client fixes → reference recording), or the references bake today's defects into the baseline. §1c gem flagged: seed-in-fight-log makes ANY player's fight replayable — candidate to fold into the seeds work. |
| 17 | Two halves: (a) the glossary as a machine-checked instrument (strict authoring rules, closed status list, code-existence check via `check-glossary.py`; the v2 renames table as plans — only `StateCorrection` user-verdicted); (b) the I1 model-pins scope from the 22.07 epic: stamina, escapes, breaks, wear | (a) server-side authored during the campaign at pace, statuses declared unagreed by its own rule 9; (b) user-authored 22.07, operationalized by the client's 1900 methodology | **HOLDS**; (b) walked position-by-position | (a) accepted ("fine so far, and fixable"); discovery: the commit-point entry is already a glossary candidate from srv 2140 §5 — the user's synchronization rider is the NEW half to write in at the rules pass (backlog); glossary pins age with the letters (re-pin item extended). (b) verdicts: stamina — ok (only `TryRestoreFishForce` rolls; visible as `fRf`); escapes — ok, the one position with a GD gate (dead throttle: fix = rebalance, pinned, decide before references) + slack-flag representativeness rider; breaks — ok after resolving the ordering question: stream split lands BEFORE recording (distribution-preserving, safe on old branch), two comparison modes (exact replay for distribution-preserving changes; distributional via `1−(1−p)^dt` for 3.2); wear — ok, no RNG at all, frozen by the stream itself (zero-forces baseline rider); bite — NARROWED OUT of campaign scope by the user: the protocol does not touch bite/generation, the coupling came only from end-to-end replay — fight entry via the existing scripted-fish mechanism (verified in `FishGenerator.cs`), `FishGenerator` stays seeded for `GenerateEscape` only, `BiteSystem` seed demoted to a §1c-replay rider. CLOSES THE RETRO QUEUE (17/17). |
