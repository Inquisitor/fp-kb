---
status: completed
executor: Stanislav Samoilov
jira: https://fishingplanet.atlassian.net/browse/FP-43631
related: FP-41746, FP-41595
---
# FP-43631: Find PCR-drop abusers in new Matchmaking

## Status
Delivered. Detection heuristic for PCR-drop abuse calibrated against 4 ground-truth abusers and applied across STEAM/PS/Xbox. Pre-finalization surgical leaderboard ban (29 abusers) on weekly period 20260504 verified 100% successful via post-finalize check — none reached the reward list. Wider sweep with 30% threshold yielded 107 candidates total, handed to Support for durable account bans. Deferred ideas (consecutive-no-show penalty, per-bracket prize caps, twink detection by IP/MAC, zero-score pivot watch, threshold drift, Mobile/NX passes) are owned by Community team or planned in separate tickets — see `backlog.md`.

## Summary
Players exploit the new matchmaking system (launched 2026-04-29) by registering for Competition-kind tournaments and not showing up, accruing `NoShowRatingPenalty` to deflate their `CompetitionRating` and drop into the lower (Nub `[0-100]`) bracket where they farm easy wins. Task: identify the cohort, estimate population size, distinguish abusers from honest losers. Output: a candidate ban-list to be applied via `Profiles.IsCompetitionsBanned` / `CompetitionsBanEndDate` while a more abuse-resistant bracket split (PCR + MaxWins / trophies cap per bracket) is designed.

## Key Findings
- `TournamentKinds.Competition = 3` (NOT 2 — confirmed in `Shared/Photon.Interfaces/Tournaments/TournamentKinds.cs`). Matchmaking grouping logic applies only to this kind.
- Rating delta is written for every participant including no-shows (`TournamentEndAdapter.CalculateTournamentRatingsGained` saves `result.Rating` regardless of `IsStarted`).
- Rating semantics: `tournament.Places` is a piecewise step-config — top places have positive `Rating`, mid/bottom places have **negative** `Rating` (smaller in magnitude than `NoShowRatingPenalty` but still negative). Sign of `r.Rating` alone does NOT distinguish no-show from honest bad finish.
- **Reliable discriminator** for no-show: `p.IsStarted = 0 OR p.IsDisqualified = 1` (penalty path `-NoShowRatingPenalty`). For zero-score: `p.IsStarted = 1 AND p.IsDisqualified = 0 AND r.Score = 0 AND r.SecondaryScore = 0` (penalty path `-ZeroScoreRatingPenalty`). For honest loss: `IsStarted = 1` and a placed-out-of-money row — `r.Rating` is whatever `tournament.Places` configured for that place (often negative).
- Cumulative PCR: `Profiles.CompetitionRating` (int). Ban hooks already present: `Profiles.IsCompetitionsBanned` (bit), `Profiles.CompetitionsBanEndDate` (datetime).
- Window start: 2026-04-29 (matchmaking enabled on Steam per FP-41595 milestone — write-pipeline pre-flight Apr 29). Leaderboards UI/rewards came up 2026-05-01 across all 5 streams.
- Abuse-signal heuristic: ratio, not absolute net rating. Abuser → `|RatingFromNoShow_DQ| ≫ |RatingFromRealPlay|`. Honest bad player → `RatingFromNoShow_DQ ≈ 0` and `RatingFromRealPlay < 0`.

## Ground Truth (provided by GD)
- `bafa56a3-af75-443c-b3d3-dee191015189` — already banned by GD; "look first" / strongest case
- `f139316b-bd01-42ba-bf31-aba105bc9bd0`
- `dab0b476-4ba7-483c-adcc-dd380df05a9d`

All confirmed by GD as 100% abusing the system. Use as validation set — any candidate detection heuristic must surface them in its top-N.

## Win Counters (gold/silver/bronze) — Storage
Lifetime counters live in `Profiles.StatsJson` (compressed JSON blob) under `$.GenericStats.<Type>.Count`. The three counters are `CompWon` (1st), `Comp2nd` (2nd), `Comp3rd` (3rd) — defined in `StatsCounterType` enum, incremented at tournament-end in `GameClientPeer_Tournaments.cs:956-964` per `tournamentFinalResult.CurrentPlayerResult.Place`. Read with `JSON_VALUE(StatsJson, '$.GenericStats.CompWon.Count')` etc. — value is string, wrap in `TRY_CAST(... AS int)`. Verified on local DB (top profile: `00872a54-...`, 16 golds).

Two views on win count, both useful:
- **Lifetime** from `StatsJson` — establishes baseline "is this player generally strong"; abuser pattern: high lifetime totals + currently low PCR is suspicious.
- **Window** (since 2026-04-29) from `TournamentIndividualResults.Place` — confirms recent farming in low bracket.

Separate signal in `CompetitiveLeaderboards*RatingsCurrent.CompetitionsWon` exists but tracks only 1st place (`Place == 1 ? 1 : 0` in `LeaderboardsAdapter.UpdateCompetitiveLeaderboards`) and is per-period; for our purposes `StatsJson` lifetime totals + window aggregation are sufficient.

## Methodology

The initial discovery-and-calibration plan completed by 2026-05-11 (task moved to operational
mode from week-2). Since then the task runs as a **weekly cycle** with a stable, calibrated
methodology — detection SQL, Mongo trajectory dump, parser agent, adversarial trial workflow,
3-layer ban execution, verification, CS/Slack/JIRA handoff, KB commit.

The full operational playbook with concrete file references for each step is in
[`methodology.md`](methodology.md). That document is hand-off ready: a new operator should be
able to run a cycle from it end-to-end without reconstructing prior context. The week-by-week
refinements ledger and the mapping to memory rules sit in that file's "Methodology refinements
over cycles" section.

The most recent fully-documented example cycle is week-7 (2026-06-22) — all referenced
artifacts in `methodology.md` point to that cycle's files.

## Milestones
- 2026-05-10: Task created. JIRA read, Slack context absorbed. Data path mapped via code (`TournamentsHelper.CalculateTournamentRatingGained`, `TournamentEndAdapter.CalculateTournamentRatingsGained`, `LeaderboardsAdapter.UpdateCompetitiveLeaderboards`) and schema (`TournamentParticipants`, `TournamentIndividualResults`, `Tournaments`, `Profiles`). Date correction from GD: matchmaking on 2026-04-29, leaderboards on 2026-05-01. Kind correction from GD: only `KindId = 3` is in scope.
- 2026-05-10: Confirmed `TournamentKinds.Competition = 3` in `Shared/Photon.Interfaces/Tournaments/TournamentKinds.cs` (`Sport = 1`, `Competition = 3`, `UserGenerated = 4` — no `2`). Confirmed PCR lives in `Profiles.CompetitionRating` (int). Per-place win counters not stored as columns (see Win Counters section); derive from `TIR.Place`. Per-platform launch dates from GD: Steam/EGS 2026-04-29; PlayStation 2026-05-06; Xbox 2026-05-07 — out-of-scope for v1 query, address in follow-up pass once Steam abusers handled.
- 2026-05-10: GD correction — `tournament.Places` is piecewise step-config, bottom-tier places have negative configured `Rating` (smaller magnitude than `NoShowRatingPenalty` but still <0). Sign of `r.Rating` alone does NOT prove no-show; reliable discriminator is `p.IsStarted = 0`. SQL unchanged (already used `IsStarted`); interpretation note updated in journal/SQL header.
- 2026-05-10: GD correction — three lifetime per-place counters DO exist: `StatsCounterType.{CompWon, Comp2nd, Comp3rd}` in `Shared/ObjectModel/Stats/PlayerStats.cs`, incremented in `GameClientPeer_Tournaments.cs:956-964` at tournament-end. Stored in `Profiles.StatsJson` under `$.GenericStats.{CompWon|Comp2nd|Comp3rd}.Count`. Verified JSON path on local (`00872a54-...`: 16 golds). Added to Query A as `LifetimeGold/Silver/Bronze`.
- 2026-05-10: Query A run on prod, all 3 ground-truth users confirmed abuse pattern: `NoShowShare` 61–78%, `NoShows` 26–50, `RatingFromNoShow_DQ` −377 to −695, `RatingFromRealPlay` +15 to +102 (all positive — abusers stay active). BAFA56A3 is the textbook tank-and-farm case (`PCR=20`, lifetime 79/63/48, +2 gold +1 bronze in window). F139316B and DAB0B476 were banned at `PCR` 926/770 respectively — GD's ban trigger fires on the no-show pattern early, before the user reaches Nub bracket. Calibration zone for threshold: `NoShows ≥ 15 AND NoShowShare ≥ 0.5 AND RatingFromNoShow_DQ ≤ -150`. Apply on full prod cohort next.
- 2026-05-10: User feedback — every `.sql` query needs leading single-line comment (becomes DataGrip tab title). Applied to both Query A and Query B. Saved as `feedback_datagrip_tab_title_comment.md` for future SQL deliverables.
- 2026-05-11: Surgical pre-finalization leaderboard ban executed across STEAM/PS/Xbox for weekly period `20260504`. Mechanism: `CompetitiveRatingsCurrent.IsBanned = 1` only — `Users.IsBanned` and `Profiles.IsCompetitionsBanned` untouched (Monday full-ban policy applies separately). Total **29** banned: STEAM 13, PS 6, Xbox 10. Threshold: `NoShowSharePct > 30%` against analysis window since matchmaking-launch per platform. Banned UserIds + per-user evidence snapshot in `artifacts/bans-2026-05-11.md`. Operational queries: D (top-N decision view), E (UPDATE under BEGIN TRAN), F (post-ban sanity), G (Community report with abuse evidence) — all in `artifacts/weekly-leaderboard-ban.sql`. Steam was the dirtiest (13/30 top dirty); proportional rot decreased on PS (6/30) and Xbox (10/30) due to shorter abuse windows.
- 2026-05-11: Investigation toolkit simplified — old Queries A and B (hardcoded ground-truth lists) dropped from `discovery-sql.sql`; redundant after threshold calibration. Canonical investigation set: **Query A** (single-user verification timeline, formerly "Query 0") + **Query B** (cohort finder with HAVING thresholds, formerly "Query C"). Workflow: B finds suspects, A verifies individual cases. Ground-truth UserIds preserved in `bans-2026-05-11.md` and journal headers for historical reference.
- 2026-05-11: Discovered post-finalize gotcha — the hourly job around `00:07-00:20 UTC` finalizes Weekly leaderboard, MERGEs eligible rows into `CompetitiveRating{Weekly|Monthly|Yearly}History` (filtering `IsBanned=0`), then **DELETEs** all rows for that period from `CompetitiveRatingsCurrent`. Result: banned users are absent from both Current AND History (Current → cleaned; History → never inserted). Audit-trail for ban operations lives **only** in our local `bans-<date>.md`.
- 2026-05-11: Operational toolkit pruned — Community report was actually produced from `Query B` output (full-cohort scan exported to Sheets with `HYPERLINK` formula for player links), not from the planned "report by hardcoded UserId list" query. That intermediate query was redundant and dropped. Final operational set in `weekly-leaderboard-ban.sql`: **D** (top-N decision view), **E** (UPDATE under BEGIN TRAN), **F** (post-ban sanity check), **G** (post-finalize verification reading from `CompetitiveRatingWeeklyHistory` — `PeriodId + TournamentKindId + DimensionTypeId + UserId` is the PK; `CompetitiveLeaderboardDimensionType` enum: `Played=1, Won=2, Rating=3`).
- 2026-05-11: Post-finalize verification confirmed **100% ban success** across all 3 platforms. Query G run on STEAM/PS/Xbox `CompetitiveRatingWeeklyHistory` for `PeriodId=20260504, TournamentKindId=3, DimensionTypeId=2`: none of the 29 banned UserIds appear in winners top-30. Reward distribution (RewardId 75066–75075) went strictly to the top-10 clean players on each platform. The PS ground-truth suspect `babasss27` (placed #3 with 3 wins and 0% NoShow) collected his reward legitimately — confirming our discriminator (`IsStarted=0`) correctly distinguishes deliberate tanking from genuine play.
- 2026-05-11: Wider residual sweep performed by lowering `NoShowSharePct` threshold to 30% (other gates unchanged). Result: **107 candidates** across STEAM/PS/XB. Full cohort handed off to Support via the shared Google Sheet for durable account bans. Operational delivery of FP-43631 complete — detection + surgical leaderboard ban (us) + durable account ban (Support).
- 2026-05-11: Reviewed deferred items with user; see `backlog.md` for the resolved state of each. Notable parked ideas worth re-examining in ~1 month: (a) consecutive-no-show penalty server-side; (b) per-bracket prize caps `MaxWins / Max2nd / Max3rd` (GDD-level, the structural fix that should make no-show abuse pointless); (c) twink/multi-account detection by IP/MAC (separate planned ticket); (d) zero-score pivot monitoring (Community-side). FP-43631 stays scoped to detection + reactive ban — these are independent follow-ups.
- 2026-05-11: Task closed. JIRA moved to Resolved by user. All deferred items routed (Community / future tickets / trigger-based watch). Final artifact set: `discovery-sql.sql` (Query A single-user verification, Query B full-cohort finder), `weekly-leaderboard-ban.sql` (Queries D/E/F/G — decision view → UPDATE → sanity → post-finalize verification), `bans-2026-05-11.md` (29-user audit snapshot).
- 2026-05-18: Week-2 follow-up cycle (task remains `completed`; treated as operational follow-up, not reopened). Pulled comparison of old (≤2026-05-10) cohort vs new (≥2026-05-11) with pro-rated thresholds for the shorter 7-day window: `NoShows ≥ 6`, `NoShowSharePct ≥ 30`, `RatingFromNoShow_DQ ≤ −90`. Verdicts: STOPPED = amnesty, STARTED = 2W ban → 2026-06-01, CONTINUED = 4W ban → 2026-06-17. New tooling added — `week2-comparison.sql` (Query B-shape report with Verdict + BanUntil columns) and `week2-ban-execution.sql` (atomic XACT_ABORT-wrapped script replicating WebAdmin "Set Competitions Banned" + LB ban + Influencer reset + AdminComment audit) and `leaderboard-ban-sync.sql` (closes the gap: `Profiles.IsCompetitionsBanned` does NOT auto-propagate to `CompetitiveRatingsCurrent.IsBanned`; only **active** Profile bans sync, expired flags left alone). Run on all 3 prod streams: 185 new Profile bans applied (STEAM 75 / PS 84 / XB 26), 150 additional LB rows synced from pre-existing Profile bans (STEAM 101 / PS 46 / XB 3). Final LB-banned rows from active Profile-banned users across W/M/Y periods: 707 total. Execution snapshot in `bans-2026-05-18.md` § Execution results.
- 2026-05-18: Architectural finding from this cycle — `Profiles.IsCompetitionsBanned` and `CompetitiveRatingsCurrent.IsBanned` are independent flags. WebAdmin "Set Competitions Banned" sets only the former, which blocks future registrations but does NOT exclude the user from current-period reward distribution (finalizer filters on the latter). A Profile-banned user with accumulated wins in current Weekly/Monthly/Yearly periods STILL receives prizes at finalization unless the LB flag is also flipped. This is the gap `leaderboard-ban-sync.sql` closes. Worth lifting into a server-side feature: option for Set-Competitions-Banned action to also propagate to LB Current. Filed as separate task FP-43844 (Story under FP-43213 Tech-Debt-2026-Q2 epic).
- 2026-05-18: Ban log backfill — 185 retroactive entries posted to Mongo `banLog` across STEAM/PS/Xbox via `ban-log-backfill-2026-05-17-{steam,ps,xbox}.js`. Mimics WebAdmin format (BanSource.WebAdmin, author "Stanislav Samoilov", reason "FP-43631 follow-up — no-show abuse (week-2 comparison)", Timestamp `2026-05-17T23:30:00Z`). Sample real WebAdmin entry checked before bulk insert exposed wrong DateTime format assumption (actual server format is `yyyy-MM-dd HH:mm:ss`, not the en-US `M/d/yyyy h:mm:ss tt` initially drafted) — saved 185 broken records. Verified counts on all three platforms match expected (75/84/26).
- 2026-05-25: Week-3 cycle — refined the question from "who no-shows a lot" to "who **drops rating AND cashes prizes**" (many players skip competitions for legit reasons; only ban those who also cash low-bracket prizes). New evidence source discovered: the Mongo **Tournament log** (`tournamentLog`, schema `main2`; `main` is stale) writes a per-competition PCR ledger — `Tournament reward Competition #X 'Name' added CompetitionRating <delta> (<before> -> <after>)`. The chain is **continuous** (every `after` = next `before`), so it captures every PCR move including no-shows. Crucially the log also stores `Player started scoring time…`, giving a self-contained no-show detector: a competition with a rating entry but no start entry = no-show. Labelling the 5 prize-zone candidates this way reversed an initial "just grinders" read — all post strong positives when playing (+178…+583) yet no-show 39–67%, pinning PCR in NOOBS/MIDDLES to cash prizes there. Tooling: `pcr-log-trajectories-2026-05-25.md` (ledgers + no-show labelling) and the unified detection/report query `week3-cs-report.sql` (no-show gate + prize gate, prize = total prizes > 3 — also drives the Support roster). Banned 4 (Steam Holekko, PS IIGot-_-Smoked, Xbox VITAO4460 + profpaulo18), uniform 2W → 2026-06-08, via `bans-2026-05-25.sql` + `ban-log-backfill-2026-05-25.js`. Djarumsuper16 (39% no-show, net climbing 167→211) reviewed and **excluded** as likely honest. Per-user record + verification in `bans-2026-05-25.md`.
- 2026-05-25: Operational gotcha (recurring) — the competition ban (`Profiles.IsCompetitionsBanned`) and the leaderboard sync (`CompetitiveRatingsCurrent.IsBanned`) are **separate transactions**. Post-ban verification caught both Xbox players still `IsBanned=0` on all periods (Steam/PS fine) — the Xbox `leaderboard-ban-sync.sql` run had not been committed (second time this happened on Xbox). Fixed with a targeted `UPDATE … IsBanned=1` and re-verified. **Standing rule:** after every ban, verify `CompetitiveRatingsCurrent.IsBanned` per platform individually, Xbox especially. Note `Main` PROD MAIN connections are read-only via DataGrip MCP — write fixes must be run by the user. (Structural fix tracked in FP-43844.)
- 2026-05-25: Week-3 Community&Support report produced — `week3-cs-report.sql` + roster `cs-report-2026-05-25.md`. Rating-drop abuse cohort (>3 prizes) for the 2026-05-18→24 window: 14 players (Steam 7 / PS 3 / Xbox 4) — 6 BANNED (our 4 + `keikoku_5816` + `IFC_BaysEmperor`), 1 REPEAT (`yaobaizhishang`, a 2024 comp-ban already expired), 7 NEW. Query adds per-bracket Play/Prize split (N/M/T), which surfaced two MIDDLES-only abusers (`yaobaizhishang`, `IFC_BaysEmperor`) as a distinct, weaker flavor vs NOOBS abusers; Verdict = ban history (NEW/REPEAT/BANNED). Handed to Support for durable account-ban decision (no BanUntil set by us). C&S query lineage: week-1 used `discovery-sql.sql` Query B (pure no-show cohort → Sheet), week-2 `week2-comparison.sql` (cross-week Verdict CONTINUED/STARTED + BanUntil), week-3 `week3-cs-report.sql` (no-show + prize gates + bracket split). Decision: keep week-3 as its own artifact, NOT yet canonical — `week3-cs-report.sql` is effectively a superset of weeks 1–2 (relax `@MinTotalPrizes=0` → week-1 wide sweep), so revisit consolidating into one canonical weekly query once methodology stabilizes after next week.
- 2026-05-25: Forward context — user plans to roll out **MaxWins (FP-43625)** Mon–Tue (≈2026-05-25/26). MaxWins = per-bracket prize cap, the structural fix that should make rating-drop abuse pointless. If it lands, the weekly rating-drop abuse sweep pivots from enforcement to **monitoring** (watch whether the abuser cohort shrinks post-rollout). Re-evaluate the reactive-ban cadence after observing one full post-MaxWins week.
- 2026-05-25: Week-3 handoff delivered, cycle complete end-to-end. C&S report finalized — `week3-cs-report.sql` / `cs-report-2026-05-25.md`: column layout aligned to the earlier report (`week2-comparison.sql` field order, **full column names, no abbreviations** — `RatingFromNoShow_DQ`/`RatingFromRealPlay`/`RatingNetDelta`/`NoShowSharePct` etc.), per-bracket Played/Prizes added as concatenated `NOOBS / MIDDLES / TOPS` strings (spaces around the slash so Google Sheets doesn't date-parse `8/12/0`), and a NEW/REPEAT/BANNED ban-history Verdict trailing. Sent the change summary to the CS lead on Slack (prize gate `> 3 prizes` narrowed 77 no-show suspects → 14; how to read NoShowSharePct / Played_NMT / Prizes_NMT / RatingNetDelta; temp draft `slack-cs-lead-2026-05-25.md` created, sent, deleted). Posted the weekly JIRA summary as comment 121247 (bans + shared-doc handoff).
- 2026-06-01: Week-4 cycle — methodology stable, **same gates** (no-show >=6/30%/-90 + prize >3) on the canonical `week3-cs-report.sql` (no query rewrite needed — confirms the query has stabilized after one week of use). Wide no-show cohort grew 77→87; tight prize-gated cohort shrunk 14→11 (Steam 7 / PS 3 / Xbox 1). Banned 5 NOOBS-farmers, uniform 2W → 2026-06-15: Steam `FurryCurrentMaster` (escalated from week-3 NEW with 4 prizes → week-4 with 12 — the prediction "if they reappear next week, ban without hesitation" played out exactly), `SplendidTroutAngler` (9 prizes), `HeitorJR2` (5); PS `vybuschna_plina` (10), `sentinel_krk` (9). All five confirmed via Mongo Tournament-log trajectory + per-competition no-show labelling — same batched-flush signature as week-3 (5–7 no-shows in one second when player logs in after offline). Per-user record + verification in `bans-2026-05-31.md`. Watchlist this week: 5 (DrakDerg / Kaneki_Ken2907 / Aorney / JuliaRybalka / Normal9459) — mixed N+M prizes, MIDDLES-only flavor (Aorney / Julia echo week-3 `yaobaizhishang`), or scattered no-shows without batch signature (Normal9459).
- 2026-06-01: Operational incidents this cycle (both fixed) — (a) Mongo backfill JS run inadvertently flushed both Steam+PS sections against PS Mongo (5 docs on PS Mongo, 0 on Steam Mongo). Fixed surgically: `deleteMany` of the 3 Steam UserIds from PS Mongo + re-ran Steam section on Steam Mongo. Final counts verified clean (3 / 2 / 0). (b) Steam Profile `BEGIN TRAN` not committed during first verification pass (mirror of last week's Xbox LB-sync gotcha); second pass came back clean after user committed. Both reinforce the standing rule: verify all three layers (Profiles / LB / banLog) per platform individually after every cycle.
- 2026-06-01: Monthly leaderboard sanity check (period 20260501) — top-10 by `CompetitionsWon` on Steam / PS / Xbox shows **3 already banned on each platform** (9 of 30 total), with the rest being clean grinders (high PeriodPCR / LifePCR, low no-show %, big positive RFRP). Best validation yet that the weekly farm-gate correctly targets top-leaderboard abusers, not random no-show players. False-positive lesson worth keeping: `VM_SAMURAI` (Steam #8, 33% no-show / 45 no-shows / RFNS −652 — flagged by raw stats) turned out to be a clean TOPS-tier grinder when checked via Mongo log (PCR climbed 2591→3301 in 13 days, only 1 isolated no-show out of 37 events, no batch signature). The **behavioural batched-flush signature via Mongo log overrules raw threshold metrics** — if Mongo doesn't show the batched pattern, don't ban regardless of how high the no-show percentage looks in SQL. Also during this check: noticed `SmirkyWord9283` Xbox ban expires 2026-06-01 — verified he's already `IsBanned=true` on current Monthly + Yearly periods, so this cycle's rewards are still blocked; no need to extend.
- 2026-06-01: Week-4 handoff delivered, cycle complete end-to-end. C&S report `cs-report-2026-05-31.md` posted to shared sheet. Slack message to CS lead sent in a more conversational register (lighter prose, less markdown, "нубики" not "NOOBS") — saved as `feedback_slack_cs_register.md` for future cycles. Posted the weekly JIRA summary as comment 122371 (bans + cohort + monthly sanity check).
- 2026-06-07: Week-5 cycle — methodology stable, canonical `week3-cs-report.sql` bumped to `@WindowStart='2026-06-01'`. Wide no-show cohort 87→76, tight prize-gated 11→14. Banned 8 — 6 NEW (2W → 2026-06-22): Steam `JuliaRybalka`, `Kaneki_Ken2907`, `emer_85_he`; Xbox `nggaaah`, `Bejk76 cz`, `Bizkit3209` — plus 2 REPEAT on a new doubled-duration policy (4W → 2026-07-06): Steam `I_MACTEP_I` (prior ban expired 2026-04-04, returned with a 30-hour continuous hourly no-show campaign sliding PCR 346 → 0 — most extreme tank seen on this task) and `TTC-SWAX` (prior ban expired exactly 2026-06-01, resumed within 6 days with three full MIDDLES->NOOBS cycles). `JuliaRybalka` and `Kaneki_Ken2907` were on week-4 watchlist with MIDDLES-only / mixed prize signatures and escalated to pure NOOBS-farming (8N+0+0 / 7N+0+0) — standing watchlist-to-ban rule applied without further deliberation. Other three from week-4 watchlist (`Aorney`, `DrakDerg`, `Normal9459`) did not reappear in the wide cohort, validating the watch-not-ban call. Per-user record + Mongo log trajectories in `bans-2026-06-07.md`; execution scripts `bans-2026-06-07.sql` + `ban-log-backfill-2026-06-07.js`; new artifact `verify-bans-2026-06-07.sql` packages the recurring 3-layer (Profiles / LB / banLog) post-ban check as a single query so the standing verify rule is one click going forward.
- 2026-06-07: **Adversarial review introduced** as a pre-execution gate — for each ban candidate a workflow spawns three subagents on the same evidence file: prosecutor (argues BAN with severity and citations), defense (argues WATCH/EXONERATE with alternative explanations), and an impartial judge that reads both arguments and renders a final verdict with confidence. Eight of nine week-5 candidates upheld for BAN (confidence 6–10); one downgraded BAN → WATCH: `Lay_D14S` (PS), where the defense accepted three points — thin sample (16 regs / 6 played), Lifetime 3/3/2 including 2 TOP prizes inconsistent with a pure NOOBS-farmer profile, and NetDelta only +42. Pattern present (three MIDDLES->NOOBS bounces, batched-flush signature), but conviction threshold not met on a first cycle; one-cycle clock — re-evaluate week-6 with a larger N. The trial mechanism stress-tests interpretation against the strongest "innocent" framing before any irreversible action and is now part of the standard weekly loop.
- 2026-06-07: Week-5 handoff delivered. C&S report `cs-report-2026-06-07.md` + per-platform TSVs (`cs-report-2026-06-07-{steam,ps,xb}.tsv`) ready for the shared sheet. **Verdict column reduced to BANNED / WATCH** for a cleaner CS picture — the underlying SQL still emits NEW / REPEAT / BANNED, but in the TSVs handed to Support all reviewed-but-not-banning rows are normalized to `WATCH` (split internally into adversarial-downgrade, MIDDLES/TOPS-only veteran flavor, and borderline-at-threshold — see `cs-report-2026-06-07.md` Reading notes). Discovered a new MIDDLES/TOPS-only veteran on PS this cycle — `Fishhunter_777` (PCR 3150, Played 0/1/15, Prizes 0/1/3 — opposite of NOOBS-farming, same flavor as `yaobaizhishang` / `JIALIN0720`). Slack message to CS lead sent in the established conversational register (proza, "нубики", short). Posted the weekly JIRA summary as comment 123435 (bans + cohort + adversarial trial + monthly sanity check).
- 2026-06-15: Week-6 cycle — methodology stable, canonical `week3-cs-report.sql` run with `@WindowStart='2026-06-08'`. Wide no-show cohort steady at ~76, tight prize-gated 14 → 15 — LARGEST cohort yet, but the adversarial trial filtered aggressively: only **4 upheld for BAN, 11 went to WATCH** (down from 8 banned last week). Banned: Steam `LaccFarro` (NEW, 2W → 2026-06-29 — the most extreme single case observed on this task so far: TOP-adjacent PCR 710 collapsed to NOOBS 50 over 3 weeks via 15 batched-flush groups and a 48.6-hour run of 23 consecutive no-shows, post-collapse parks in NOOBS at 8N+1M+0T prizes); PS `Mr-crimson-21` (NEW, 2W — 9 MIDDLES->NOOBS drops, most of the cohort; pure 7N+0+0); plus 2 REPEAT on the doubled-duration policy (4W → 2026-07-13): PS `STARI40K_YT` (prior ban expired 2026-06-01, immediately sat PS weekly leaderboard #1 by wins at 5W/15P with pure NOOBS prize haul) and PS `IIGot-_-Smoked` (our own week-3 ban expired EXACTLY 2026-06-08, returned inside six days with three full climb-then-flush MIDDLES->NOOBS cycles including a 3-penalties-in-2-seconds flush on 2026-06-14). All 4 in their platform weekly LB top-10 by wins — detection continues to land on actual prize-zone abusers. Per-user record + Mongo log trajectories distilled in `pcr-log-trajectories-2026-06-14/<userId>-<slug>.md` cards (15 cards, ~100 KB total); raw 7.6 MB Mongo dumps were transient buffer for the parser agent and were not kept — `pcr-trajectory-queries-2026-06-14.js` is the recipe to regenerate them if needed.
- 2026-06-15: Two methodology refinements crystallized this cycle. (a) **Fresh-lifetime + 3+ wins is necessary but not sufficient for BAN** — the trial established the joint test: 3+ wins this week, FRESH lifetime (<15 prizes), NET-NEGATIVE PCR over the cycle, AND demonstrated MIDDLES exposure (climbs to >=100 followed by no-show drops back below). Several LB-top-10 candidates passed criterion 1 but failed the joint test: `Kacumi` (Steam, lifetime 5, net +94 climbing into MIDDLES — climber not farmer); `MikeikeMOON` (Xbox, lifetime 4, PCR range 0..45 entirely within NOOBS — no bracket cliff to exploit); `ArTeM209` (Steam, lifetime 11, sawtooth PCR keeps clawing back up — not parking). (b) **Watchlist-to-ban escalation requires a flavor change to NOOBS** — `JIALIN0720` (week-5 watchlist holdover) returned with 15 batched flushes and 44h streak but 0 NOOBS drops and 0 NOOBS prizes — same MIDDLES-only flavor he held in week-5. The watchlist-to-ban rule does NOT trigger on MIDDLES-flavor persistence alone — only on actual flavor change (NOOBS prizes appear, MIDDLES->NOOBS drops appear). 6 of 7 week-5 watchlist players did not reappear at all in week-6 wide cohort — validates the week-5 watch-not-ban calls.
- 2026-06-15: **`IsCompetitionsBanned` flag semantic discovered** while sanity-checking Support actions against the cohort. The canonical game-engine check (`ProfileLogic.IsCompetitionsBannedNow()`) is the AND of `IsCompetitionsBanned=true` AND `CompetitionsBanEndDate > UtcNow`. A stale flag with `BanEnd` in the past OR `BanEnd=NULL` means the player is NOT effectively banned — game registration logic allows them through. Two such stale-flag rows in week-6 cohort: `VM_NPWP` (Steam, BanEnd 2026-06-07 in past — Support ban from `yuliya.chumak` expired) and `IFC_BaysEmperor` (Steam, BanEnd NULL — legacy artifact, banLog shows only AsyncProcessor MAC bans, no Competition ban). Both were demonstrably playing at the time of cohort selection (`IFC_BaysEmperor` on weekly LB #25, `VM_NPWP` on weekly LB #4 with NET +412 PCR climb). Documented in `feedback/competitions_banned_semantic.md` and the methodology section of `bans-2026-06-14.md`. **SQL re-ban WHERE clause gotcha**: the current form `ISNULL(IsCompetitionsBanned, 0) = 0 OR (BanEnd IS NOT NULL AND BanEnd <= GETUTCDATE())` does NOT cover stale-flag-with-NULL-date rows; correct form is `NOT (IsCompetitionsBanned = 1 AND BanEnd > GETUTCDATE())` (mirrors the game-engine check). Will adopt the correct form in week-7's `bans-YYYY-MM-DD.sql` template.
- 2026-06-15: Support-action cross-check. Of 15 candidates in our wide cohort, only one had a Support-issued Competition ban in Mongo banLog — `VM_NPWP` (banned by `yuliya.chumak` 2026-05-07, expired 2026-06-07, no reason in log). Two others (`JIALIN0720`, `IFC_BaysEmperor`) had `Full ban via AsyncProcessor` MAC/IP bans from 2025 but no Competition ban — these are an anti-cheat track, orthogonal to rating-drop methodology. Trial-Support alignment within the rating-drop domain: 14/14. `VM_NPWP`'s reappearance in our cohort with a markedly softer signal than typical (net +368 PCR climbing, prizes 2N+9M+0 MIDDLES-skewed, strong TOPS-tier wins) — combined with our decision not to re-ban — is consistent with the original Support action having achieved its goal: he's either adjusting his behavior post-ban or playing more cautiously. The original Support call looks well-placed in hindsight.
- 2026-06-15: Week-6 handoff delivered. C&S report `cs-report-2026-06-14.md` + per-platform TSVs (1 BANNED + 7 WATCH Steam, 3 BANNED + 3 WATCH PS, 1 WATCH Xbox). Slack to CS lead sent in the established conversational register; week-6 calibration refined the `feedback_slack_cs_register.md` with six new pattern adjustments (standalone courtesy-opener paragraph for off-hours sends, "метагейм"/"банвейв"/"WATCH-лист" as preferred terms, frame lists as cross-cycle comparison rather than delegated work). Posted the weekly JIRA summary as comment 124271 (bans + cohort funnel + Support-cross-check + leaderboard sanity).
- 2026-06-22: Week-7 cycle — **largest single-cycle pass on this task so far**. Wide cohort jumped 15 → 26 — biggest yet, breaking the "starting to clean up" trend from weeks 5-6. Banned 17 — 12 NEW (2W → 2026-07-06) + 5 REPEAT (4W → 2026-07-20). Steam 7: `TF_B4ngwal`, `Ramboo051`, `Audrey_HH` (**145-hour / 6-day continuous no-show streak — longest single-account dormancy event observed on this task**), `OZBULLDOG`, `VovaTemniy` (NEW) + `FurryCurrentMaster` (REPEAT — our own week-4 ban expired EXACTLY 2026-06-15, re-engagement in the cohort within hours), `Dokidepp` (REPEAT). PS 8: `jorja09` (**10 NOOBS prizes + 10 MIDDLES->NOOBS drops — most decisive single-cycle case in the cohort, trial conf 10/10**), `rabolio41100` (week-6 watchlist escalator, flavor changed to pure NOOBS), `maminapokorny83` (week-5 watchlist escalator, same flavor change), `Neterrall`, `Fat_tuna_mama`, `strullendorfer` (NEW) + `Flo-GrayFOX`, `bostonbroncos24` (REPEAT). Xbox 2: `LEBOOGIEEEE` (NEW — 48 no-shows, highest absolute count of cohort), `BuzzingLemur417` (REPEAT). Five REPEATs is the most this task has seen in one cycle; three of them had Support comp-bans expire exactly 2026-06-01, one had a 2.5-month-old expiry, one had our own week-4 ban expire 06-15. SQL re-ban WHERE clause adopted the new form `NOT (IsCompetitionsBanned = 1 AND BanEnd > GETUTCDATE())` from the week-6 gotcha — mirrors the canonical game-engine `IsCompetitionsBannedNow()` check, covers stale-flag-with-NULL-date rows correctly.
- 2026-06-22: **Adversarial trial verdicts: 21 BAN + 5 WATCH + 0 EXONERATE on 26-row cohort** (78-agent workflow). Of the 21 BAN verdicts, 4 were already actioned by Support before the sweep (`Kacumi`, `poink`, `A-J-Rimmer-BSC`, `nowa_zajawka`) — not re-banned, their Support BanEnd runs past the 2W standard. Trial confirmed all 4 at confidence 9-10/10. **Kacumi calibration case**: she was on the week-6 watchlist with trial conf 7 WATCH (defended on net +94 PCR climbing into MIDDLES); Support banned her durably within a week, and the week-7 trajectory showed the bracket-farming signature was always real (10 batched flushes + 4 MIDDLES drops + 24.5h streak + PCR drained 38->14). Methodology refinement: "net positive PCR" alone does not defeat the bracket-farming hypothesis when the climbs are followed by no-show flushes and re-engagement at NOOBS-bracket competitions — the climbs are the up-arc of a climb-and-cash cycle, not climber behavior. Going forward, "climb then flush then play NOOBS at low PCR" should be flagged as the climb-and-cash pattern, not exonerated as climbing. **Trial-Support alignment in the rating-drop domain: 18/18 across two cycles** — both VM_NPWP (week-6, orthogonal anti-cheat domain) and the four week-7 Support actions are aligned with the calibrated methodology.
- 2026-06-22: Watchlist-to-ban escalation rule fired cleanly for the second consecutive cycle. `rabolio41100` (week-6 watchlist holdover, was deferred on skill-ceiling oscillation interpretation) and `maminapokorny83` (week-5 watchlist holdover, was borderline at 4N prizes) both showed clear flavor change to pure NOOBS this week — 8 and 6 NOOBS prizes respectively, zero MIDDLES — and went in without further deliberation. Standing rule validated: deferred-conclusion cases on the watchlist resolve cleanly given one more cycle of data. Five candidates kept on watchlist this cycle: `Matiamo_PL` (mixed N+M with net-positive PCR, highest activity volume of cohort at 67 played, only WATCH row sitting in a weekly LB top-10 at PS #8), `ST-9257`/`TR-dennisfb`/`Tight_LinesJoe65` (TOP/MASTERS-tier sandbagging flavor — different mechanism from NOOBS-farming methodology target), `TheFastestDevil` (climbing from PCR 0, weak signal, one-cycle clock). `Tight_LinesJoe65` is a stale-flag row (IsBanned=true with BanEnd 2025-05-10) — not effectively banned per game-engine check — confirmed the week-6 stale-flag pattern.
- 2026-06-22: Week-7 handoff delivered. C&S report `cs-report-2026-06-21.md` + per-platform TSVs (9 rows Steam: 7 BANNED by us + 2 BANNED by Support / 15 rows PS: 8 BANNED + 2 BANNED by Support + 5 WATCH / 2 rows Xbox: 2 BANNED). Distilled trajectory cards saved to `pcr-log-trajectories-2026-06-21/<userId>-<slug>.md` (26 cards, ~200 KB total); the consolidated raw Mongo dumps (~15 MB in 3 per-platform TSVs) were transient parser-agent buffer and were not kept — `pcr-trajectory-queries-2026-06-21.js` is a single aggregate query against `$in: [...]` of all 26 UserIds, run once per platform Mongo. Slack to CS lead sent in the established conversational register. Posted the weekly JIRA summary as comment 125330 (bans + cohort funnel + Support-cross-check + leaderboard sanity); JIRA comment rewritten in impersonal voice per Stan's preference (no `we`/`our`/`us` — saved as `feedback_jira_impersonal_register.md`).
- 2026-06-29: Week-8 cycle — wide cohort 26 → 24 (post-spike stabilization, basically flat from week-7), banned 17 → 15 (14 NEW 2W → 2026-07-13 + 1 REPEAT 4W → 2026-07-27). REPEAT count dropped 5 → 1 — consistent with the read that week-7 cleaned the immediate-recidivism wave around 2026-06-01 ban expiries. Steam 5: `ArmlessFisherMan`, `BB_Anastasia` (74 regs — highest activity volume of cohort), `MonsterFish_fuark` (week-6 watchlist escalator — flavor changed from MIDDLES-veteran to 7N+0+0), `angeperdu` (was week-5 borderline WATCH), `krolikusik`. PS 9: `Matiamo_PL` (**week-7 watchlist escalator — was mixed 5N+3M+0 net +100, now 8N+0+0 net -111 with 13 MIDDLES->NOOBS drops, most in cohort; PS weekly LB #1 by wins with WeeklyScore -64 at sweep time**), `tigrou_le_boss42`, `M4R5H_57_` (79% no-show — highest %), `Epic70cosmin` (72% no-show), `MrChadRico`, `MONSTER-VERT1325`, `TR-BILECIKLI_CMR`, `kokoljj`, `IKIGAI__1__` (REPEAT — prior ban expired 2026-06-17, abuse resumption 11 days later). Xbox 1: `Smooter85` (**135.8-hour / 5.6-day continuous no-show streak — surpasses week-7 Audrey_HH's 145h record as the longest single-account dormancy event observed on this task**). Per-user record + Mongo log trajectories distilled in `pcr-log-trajectories-2026-06-28/<userId>-<slug>.md` (24 cards); execution scripts `bans-2026-06-28.sql` + `ban-log-backfill-2026-06-28.js` + `verify-bans-2026-06-28.sql`. 3-layer verify clean on all three platforms.
- 2026-06-29: **Adversarial trial verdicts: 17 BAN + 7 WATCH + 0 EXONERATE on 24-row cohort** (72-agent workflow). Of the 17 BAN verdicts, 2 were already actioned by Support before the sweep (`Adlerblut-Slayer` Support BanEnd 2026-07-28, `TR-dennisfb` 2026-07-23) — not re-banned, their Support BanEnd runs past the 2W standard. Trial confirmed both at confidence 10/10. **TR-dennisfb is a calibration twin of Kacumi week-6 → 7**: he was the week-7 trial WATCH on MASTERS-sandbagging-flavor grounds (PCR 1004 → 32 with MIDDLES-only prizes); on week-8 the prize flavor changed to 6N+0+0 pure NOOBS, and Support caught the shift before our sweep. Same one-cycle pattern as Kacumi (week-6 WATCH → week-7 Support-banned). Methodology refinement crystallized: **high-PCR sandbagging WATCH should carry a one-cycle clock — explicit recheck for week+1 with elevated BAN bias if NOOBS prizes appear OR if MIDDLES->NOOBS drops first appear**. Going forward the WATCH default for TOP/MASTERS-tier sandbaggers is flagged with this clock. **Trial-Support alignment in the rating-drop domain: 20/20 across three cycles** — both week-8 Support pre-actions confirmed BAN at the highest confidence band.
- 2026-06-29: Second methodology refinement this cycle — **novice-deference rule for first-cycle NEW with sparse lifetime**. `KingYakO2` (Steam, NEW first-cycle, lifetime 1/1/5 = 7 total prizes ever) showed surface-strong NOOBS signature (7N+0+0 prizes, 53% no-show, PCR ended at 0) but trial gave WATCH conf 7 on dispositive defense pillars: (a) re-engagement +30/+23/+35 PLAYED at PCR 78 right before going offline is structurally inconsistent with a strategic flush — a flusher does not voluntarily reclimb to 78 right before logging out, that destroys the reset just executed; (b) batched flushes at identical timestamps read as scheduler-sweep reconciliations not discrete player decisions; (c) novice lifetime profile is not the skilled-smurf class methodology rule #1 targets; (d) net RFRP - RFNS = -57 is the wrong direction for the asymmetry signature. Leaderboard sanity check confirmed: rank 162 on monthly LB with 1 win — far below farmer-tier volume. Refinement: **NEW first-cycle candidates with novice lifetime (TotalPrizes < 10) warrant WATCH with elevated week+1 escalation bias if NOOBS pattern persists** — same shape as the high-PCR clock above. Watchlist escalation rule also fired cleanly for the third consecutive cycle — `MonsterFish_fuark` (week-6 holdover, MIDDLES-veteran flavor → 7N+0+0) and `Matiamo_PL` (week-7 holdover, mixed N+M net +100 → 8N+0+0 net -111) both went straight to BAN without further deliberation.
- 2026-06-29: Watchlist this cycle: 9 (vs 5 week-7) — more borderline-mixed cohort drove the count up. Breakdown: 4 TOP/MASTERS-tier sandbagging flavor with one-cycle clock (`VM_Vigor` Steam PCR 975 week-6 returning, `ST-9257` PS PCR 924 week-7 returning, `Ttv_s4muka019` PS PCR 911 week-6 returning, `LZ23J7KS` PS MASTERS PCR 2250 with 25 batched flushes but 0 MIDDLES drops — sandbagging entirely within MASTERS); 1 MIDDLES-only veteran (`serber-denis85` PS, lifetime 224 prizes, stale ban flag 2026-06-01); 1 novice with sample-size deference (`KingYakO2` per refinement above); 1 ascending-peak climber pattern (`BarNoneD` Xbox — net +111 from PCR 0, prizes 5N+0+0 but each flush followed by a HIGHER subsequent peak 47 → 115 → 122 → 135, with +22/+37 deltas on MIDDLES-PLAYED entries showing genuine competition — climber-with-erratic-attendance economics, not closed-cycle farming); 2 post-trial new arrivals not adversarial-reviewed (`alanAuma` Steam — TOP-flavor matches sandbaggers, `Lesky2123` Xbox — 6N pure NOOBS but joined cohort after trial closed). The 7 trial-WATCH carry their respective one-cycle clocks for week-9.
- 2026-06-29: **Monthly leaderboard sanity check added** to the standard cycle — period 20260601 top-50 by Wins across all three platforms ahead of the monthly payout. Findings: 10 of the week-8 cohort sit in monthly top-50 (after the ban-pack lands, 9 will show LbBanned=true and forfeit monthly prizes); 16 active bans in monthly top-50 are correctly LB-synced (FP-43631 history visible on all three platforms — `Ramboo051`/`Dokidepp` Steam, `rabolio41100`/`Neterrall`/`Mr-crimson-21` etc. on PS, `LEBOOGIEEEE`/`BuzzingLemur417` on Xbox, plus Support-actioned `Kacumi`/`poink`/`A-J-Rimmer-BSC`/`nowa_zajawka`); 25 PROF_STALE_FLAG rows whose profile bans expired in the month so LB IsBanned=false, but who passed the week-8 detection gate cleanly (playing without no-show abuse post-ban) and will auto-surface as REPEAT candidates only if abuse resumes. The two monthly-LB-top-10 cohort entries that will be filtered after this ban-pack: `angeperdu` Steam #8 and `Matiamo_PL` PS #3. One stale-flag row in PS monthly top-10 (`Panonski_Alas` #5, BanEnd 2026-06-01 expired) will receive monthly prize — formally not effectively banned per the game-engine semantic; flagged in the CS handoff for monthly-prize policy discussion separately, not actionable by this task.
- 2026-06-29: Week-8 handoff delivered. C&S report `cs-report-2026-06-28.md` + per-platform TSVs (8 rows Steam: 5 BANNED by us + 3 WATCH / 15 rows PS: 9 BANNED + 2 BANNED by Support + 4 WATCH / 3 rows Xbox: 1 BANNED + 2 WATCH). 26 TSV rows total (17 BANNED + 9 WATCH) — TSV row count diverges from the 24-row trial cohort because of two post-trial new arrivals that joined the SQL gate window between sweep and post-ban refresh; both marked WATCH for accuracy with a note that they were not adversarial-reviewed this cycle. Slack to CS lead sent in the established conversational register, including a heads-up paragraph on the upcoming monthly leaderboard rollover (two cohort members in monthly top-10 will be filtered; one stale-flag row remains formally eligible). Posted the weekly JIRA summary as comment 126658 (bans + cohort funnel + Support-cross-check + monthly LB sanity check). Methodology playbook `methodology.md` updated with the Mongo banLog backfill ship-with-active-inserts rule (the `insertMany([...])` blocks ship without `//` prefix; the operator selects them with the `var TS/MSG_NEW/MSG_REPEAT` assignments in one highlight and comments them out post-execution to make accidental re-runs harmless).
- 2026-06-30: Monthly leaderboard sanity intervention. CS lead flagged that Steam has a patch auto-syncing Profile.IsCompetitionsBanned to CompetitiveRatingsCurrent.IsBanned in both directions — so on ban-expiry the LB IsBanned would revert to 0 mid-month and expired-ban players would collect monthly prizes. Prepared `monthly-lb-ban-june-2026.sql` (atomic XACT_ABORT UPDATE of Monthly `20260601` + Yearly `20260101` IsBanned=1 for the 50-UserId cohort of everyone banned by FP-43631 or Support during June: 44 our bans across weeks 5-8 + 6 Support pre-actioned) plus standalone `monthly-lb-ban-june-2026-verify.sql`. On the actual run against the three PROD MAIN however the read-only verify returned OK for all 50 (7 already-expired-Profile-ban rows included) — the auto-sync patch either not yet deployed or doesn't propagate the unban step, so `leaderboard-ban-sync.sql` set IsBanned=1 at ban time and it never got unset. No UPDATE was committed. Scripts kept in `artifacts/` for future cycles if the patch reaches prod with the unban propagation.
- 2026-07-06: Week-9 cycle — **wide cohort halved 24 -> 12** (largest cycle-over-cycle shrink to date), banned 15 -> 6 (6 NEW 2W -> 2026-07-20 + 0 REPEAT). **Xbox cohort empty for the first time** on this task -- three cycles of banwaves have caught up with the exploit population on that platform. **First cycle without a REPEAT** since the recidivism tracking started at week-5 -- confirming the 2026-06-01 ban-expiry recidivism wave was fully processed in week-7. Steam 3: `Ti_To` (**PCR 668 -> 38 = -630, largest single-cycle collapse observed on this task**; 76 NO-SHOWs, 18 batched flush groups highest of cohort), `H.a.r.y44` (**heavy veteran 178 lifetime prizes with flavor change to pure 6N NOOBS**; 10 MIDDLES->NOOBS drops highest of cohort -- same sandbagger-shifts-to-NOOBS pattern family as Kacumi week-6->7 and TR-dennisfb week-7->8), `IaMiya`. PS 3: `EL-_-Diablo-_-51` (Kacumi refinement fires -- net-positive PCR +30 but 7 MIDDLES->NOOBS drops + 10 batched flushes at bracket boundary), `San-miculsan`, `kevynrdm13`. Per-user record + Mongo log trajectories distilled in `pcr-log-trajectories-2026-07-05/<userId>-<slug>.md` (12 cards); execution scripts `bans-2026-07-05.sql` + `ban-log-backfill-2026-07-05.js` + `verify-bans-2026-07-05.sql`. 3-layer verify clean on both platforms.
- 2026-07-06: **Adversarial trial verdicts: 8 BAN + 4 WATCH + 0 EXONERATE on 12-row cohort** (36-agent workflow). Of the 8 BAN verdicts, 2 were already actioned by Support before the sweep (`ArTeM209` Support BanEnd 2026-08-04, `Gustyn112` 2026-08-05) -- not re-banned, their Support BanEnd runs past the 2W standard. Trial confirmed both at confidence 10/10 and 9/10. **Trial-Support alignment in the rating-drop domain: 22/22 across four cycles** -- both week-9 Support pre-actions confirmed BAN at the highest confidence band. **Gustyn112 data anomaly**: parser detected 10 NO-SHOWs in Mongo tournament log vs 15 in the week-9 SQL (5-row discrepancy). Trial noted but did not block the BAN verdict (severity was sufficient regardless). Possible explanations: Support pre-action redacted or hid the 5 tail ledger entries, tail entries appeared post-parser-run, or parser regex missed a variant. Not investigated further this cycle -- flagged for future forensics if the pattern recurs on other Support pre-actioned candidates.
- 2026-07-06: **Novice-deference rule (week-8 KingYakO2 refinement) fired twice cleanly** this cycle at exactly the lifetime = 10 threshold. `CreekSamurai` (Steam, Level 62, lifetime 2/2/6 = 10 total) with 7 pure NOOBS prizes but trajectory PCR 31 -> 104 climbing (+93 net) and only 3 MIDDLES->NOOBS drops. `FarantirPL` (Steam, Level 86, lifetime 1/5/4 = 10 total) with 5 pure NOOBS + 3 MIDDLES->NOOBS drops (surface strong) BUT trial accepted three defense pillars: (a) 07-01 recovery arc PCR 3 -> 113 with 5 consecutive PLAYED entries (opposite of harvest-and-abandon), (b) 06-30 08:00 -> 07-01 00:00 = one continuous 16h absence block not spaced tactical flushes, (c) 07-04 batched cluster contains a PLAYED +4 mixed with NO-SHOWs (server-closeout artifact not intentional flush). Both got WATCH conf 6 (weakest confidence in cohort). Rule 6 remains sound and self-consistent -- both under one-cycle clock for week-10 escalation if NOOBS pattern persists.
- 2026-07-06: **VM_Vigor now third consecutive cycle in the wide cohort** (week-6 WATCH, week-8 WATCH, week-9 WATCH) with unchanged TOP-flavor (0 NOOBS prizes, 0 MIDDLES->NOOBS drops in each 3-week trajectory window, always net-positive climbing inside MIDDLES/TOP tier). Genuinely a MIDDLES/TOP sandbagger, a different mechanism from the NOOBS-farming enforcement target. The week-8 one-cycle-clock refinement for high-PCR-sandbagging WATCH says re-check with elevated BAN bias if NOOBS prizes appear -- but they never appear. Methodology question deferred to post-week-10 review: at what point does persistent WATCH close as "not our target" rather than remain a rolling flag? Panonski_Alas (PS) shows the same profile at MASTERS-tier (PCR 1025 climbing, 0 drops, 0N+7M+1T) -- same family.
- 2026-07-06: Week-9 handoff delivered. C&S report `cs-report-2026-07-05.md` + per-platform TSVs (8 rows Steam: 3 BANNED by us + 2 BANNED by Support + 3 WATCH / 4 rows PS: 3 BANNED + 1 WATCH); Xbox skipped as cohort was empty. 12 TSV rows total (8 BANNED + 4 WATCH). Slack to CS lead sent in a shorter form than prior cycles (per Stan's preference for a compact overview with details deferred to the ticket/sheet). Posted the weekly JIRA summary as comment 127530 (bans + cohort funnel + Support-cross-check + watchlist breakdown by mechanism family).
- 2026-07-13: Week-10 cycle -- **wide cohort rebounded** 12 -> 19 (+58%) after 3 cycles of shrinkage, but our banned count continued shrinking 6 -> 3 (**smallest ban count on task**: 2 NEW + 1 REPEAT; Support pre-actioned 7 out of 9 trial-BAN cases before our sweep, historic high). Xbox rebounded from 0 to 4 cohort rows. Steam 1: `Captain_Djack_Sparrow` (NEW 2W -> 2026-07-27, PCR 218 -> 62, 3 MIDDLES->NOOBS drops, 7 batched flushes). Xbox 1: `alphaBiTsoop16` (NEW 2W -> 2026-07-27, **79% no-show rate extreme**, PCR 54 -> 0 collapse). PS 1: `Miron_33` (REPEAT 4W -> 2026-08-10, **new pattern signature: sink-comp repeat targeting** -- Akula #372479/#372516 and Labirint #372504/#372587 each NO-SHOWed twice, deliberate re-selection of drain target that variance and server-flush hypotheses cannot explain). Per-user record + Mongo log trajectories distilled in `pcr-log-trajectories-2026-07-12/<userId>-<slug>.md` (19 cards); execution scripts `bans-2026-07-12.sql` + `ban-log-backfill-2026-07-12.js` + `verify-bans-2026-07-12.sql`.
- 2026-07-13: **Adversarial trial verdicts: 9 BAN + 10 WATCH + 0 EXONERATE on 19-row cohort** (57-agent workflow). Of the 9 BAN verdicts, 6 Support pre-actioned trial-confirmed (JFF_Gothyka conf 10, LaccFarro conf 10 -- our week-6 recidivist, CreekSamurai conf 8 -- w9 novice-deference validation, MLG720YOLO conf 10, Da Sneaky Snake conf 10, CraddiePoosta conf 9) + 1 Support pre-actioned trial-dissent (sandaljepitt WATCH conf 7 -- first miss on alignment counter). **Trial-Support alignment on rating-drop: 27/28 across four cycles** (first dissent this cycle). LaccFarro operational note: our WHERE clause correctly skipped his Profile update because Support 5W (BanEnd 2026-08-12) already exceeds our proposed 4W-REPEAT (2026-08-10); Mongo banLog audit entry inserted anyway (record of intent). De facto banned to 2026-08-12 by Support standard. **Our week-6 recidivism call was validated: Support caught the same pattern post-expiry of our BAN**.
- 2026-07-13: **Da Sneaky Snake set three absolute records on this task simultaneously**. Support pre-actioned 5W to 2026-08-11 on Xbox. **13 NOOBS prizes in one cycle** (previous record 12 from Adlerblut-Slayer w8, then A-J-Rimmer-BSC w7=11); **PCR 975 -> 0 collapse of -1004** (largest observed on this task); **79h continuous no-show streak** (previous record 5.6 days = 135h from Smooter85 w8; the streak metric is now expressed in hours, this is 79h = 3.3 days but consecutive-comp count of 37 is the new absolute record). Trial CONCEDE at doubt 2/10.
- 2026-07-13: **First Trial-Support dissent on rating-drop: sandaljepitt**. Support pre-actioned 2W to 2026-07-26. Trial gave WATCH conf 7 under rule 6 novice-deference (Lifetime 5 total) plus load-bearing structural argument: PCR range 0..69 entirely inside NOOBS bracket, 0 MIDDLES->NOOBS drops (no cross-bracket signature). Support duration (2W, not permanent) confirms rating-drop vector, not cheat -- cheat bans on FP are permanent. Support saw enough to act; trial framework did not. Alignment counter first miss: 27/28. **Methodology calibration flag**: rules 1/4/6 all require cross-bracket signature (MIDDLES->NOOBS drops OR climb-then-flush arcs) as load-bearing evidence, but real abuse can happen entirely within NOOBS bracket (repeated register-and-skip while staying below 100). **Candidate rule 9** (within-bracket detector): high no-show volume + pure NOOBS prize concentration + NO cross-bracket movement + persistent low PCR. To discuss with CS lead and add if pattern recurs.
- 2026-07-13: **CreekSamurai completed the week-9 -> week-10 novice-deference validation cycle**. Gave WATCH conf 6 in week-9 on Lifetime=10 + recovery-climb + 3 MIDDLES->NOOBS drops. Pattern persisted in week-10: Lifetime grew to 16 (still borderline); 8 MIDDLES->NOOBS drops up from 3; 33 NO-SHOWs. Support pre-actioned 2W matching our rule 4 auto-BAN trigger under rule 6/8 combined. This validates rule 6 as a functional off-ramp: WATCH first cycle, auto-BAN on pattern persistence. Rule 6 -> rule 4/8 escalation ladder works. FarantirPL (the other w9 novice-deference case) had the opposite outcome -- did not return in the w10 cohort, confirming his WATCH was correct.
- 2026-07-13: **Second methodology refinement this cycle: sink-comp repeat targeting**. Miron_33 (PS, stale REPEAT from 2025-10-10, borderline 33% no-show, mixed 4N prize signal) would have been ambiguous under standard rules, but trial surfaced a smoking-gun pattern: the SAME competition ID (Akula #372479 and #372516; Labirint #372504 and #372587) got NO-SHOWed in TWO separate registrations by the same UserId. Variance and server-flush artifact hypotheses cannot explain deliberately re-selecting the same drain target. Rule addition candidate: sink-comp repeat targeting is a rule-1 signature strong enough to override sample-size objection on candidates with weak overall trajectory. BAN 4W under REPEAT tier applied.
- 2026-07-13: Week-10 handoff delivered. C&S report `cs-report-2026-07-12.md` + per-platform TSVs (10 rows Steam: 1 BANNED by us + 3 BANNED by Support + 6 WATCH / 5 rows PS: 1 BANNED by us + 1 BANNED by Support + 3 WATCH / 4 rows Xbox: 1 BANNED by us + 2 BANNED by Support + 1 WATCH). 19 TSV rows total (9 BANNED + 10 WATCH). Slack to CS lead sent in the compact shorter form; explicitly flagged the sandaljepitt Trial-Support dissent for post-cycle discussion. Posted the weekly JIRA summary as comment 129558 (bans + cohort funnel + Support cross-check + sandaljepitt dissent + Da Sneaky Snake records + CreekSamurai novice-deference validation + Miron_33 sink-comp pattern). Codex second-opinion consultation on the methodology deferred to post-cycle: key takeaways (documented for followup) -- (a) methodology.md is stale (rules stop at 5, alignment counter shows 18/18 vs actual 27/28), (b) two-phase verdict split recommended to reduce prosecutor/defense/judge correlated reasoning, (c) alignment counter is sanity check not validation metric -- real falsification test requires blind replay, (d) blind spots identified: gate evasion, flavor camouflage, boundary camouflage (sandaljepitt exactly this), novice shield, batch camouflage, start-but-throw pivot, data-source SQL/Mongo disagreement recurring.
- 2026-07-20: Week-11 cycle -- wide cohort basically flat: 19 -> **20** (+1). Banned by us continues small uptick 3 -> **5** (4 NEW 2W -> 2026-08-03 + 1 REPEAT 4W -> 2026-08-17). Steam 1: `FoxMilard` (NEW, LB Rank 27, Lifetime 62 heavy grinder with **NOOBS flavor-change** -- 4 climb-and-cash cycles + 4 MIDDLES->NOOBS drops, rule 1 flavor-shift override + rule 5 Kacumi stack, trial conf 9/10). PS 3: `CFC-T-W-T-32568` (NEW, **strongest single verdict of cycle conf 10/10**, textbook farmer LB Rank 8 with 6N pure NOOBS harvest at PCR 4, defense CONCEDE at doubt 2/10), `Chuydakid214` (NEW, 4 climb-then-flush cycles peaks 187/165/152, 2 MIDDLES->NOOBS crossings), `strullendorfer` (REPEAT -- **our week-7 NEW 2W BanEnd 2026-07-06 expired, immediate recidivism 13d later**, rule 1+4+5+8 stack, defense CONCEDE, conf 10/10). Xbox 1: `Belion019` (NEW, 4 Kacumi climb-and-cash cycles + 6 batched flushes + 1 M->N drop + 67% NS on rating-drop grounds; **88 CHEAT triggers active in trajectory (Undriven boat / Fish catch distance / Line high extension) treated as ORTHOGONAL** -- AntiCheat framework separate scope, rules fire independently). Per-user record + Mongo log trajectories distilled in `pcr-log-trajectories-2026-07-19/<userId>-<slug>.md` (20 cards); execution scripts `bans-2026-07-19.sql` + `ban-log-backfill-2026-07-19.js` + `verify-bans-2026-07-19.sql`. 3-layer verify clean on all three platforms.
- 2026-07-20: **Review verdicts: 9 BAN + 11 WATCH + 0 EXONERATE on 20-row cohort** (60-agent workflow). Of the 9 BAN verdicts, 4 Support pre-actioned trial-confirmed (yevhen331 conf 9, sen1a conf 8, evgeniy3311 conf 9, Ricky27sampei conf 7) + 1 Support pre-actioned trial-dissent (**LZ23J7KS WATCH conf 7 under rule 7 direction 2 -- 2ND Trial-Support dissent on rating-drop, alignment counter 32/34**). Support pre-actioned duration: LZ23J7KS 2W confirms rating-drop vector not cheat (cheat bans permanent). **LZ23J7KS is empirical validation of rule 7 direction 2 (post-Codex formalization) for closure**: TOP-flavor MASTERS sandbagger family (LZ23J7KS/JIALIN0720/Bas_di08/VM_Vigor/Panonski_Alas) should exit FP-43631 wide-cohort tracking -- separate anti-abuse scope. Support scope broader than our NOOBS-farming methodology target.
- 2026-07-20: **Rule-6 novice-deference ladder validated a SECOND time**: evgeniy3311 completed W10 -> W11 escalation cycle. Was WATCH conf 7 in week-10 on Lifetime 10 with partial structural counter-evidence. Pattern persisted+escalated in W11: Lifetime grew to 21 (+11 NOOBS prizes this window), 26 NS 63%, PCR 55. Support pre-actioned at 2W duration matching rule 4 auto-BAN trigger under rule 6/8 combined. First validation was CreekSamurai (w9->w10). **Off-ramp confirmed: WATCH first cycle, auto-BAN on pattern persistence under rule 8 thresholds**. MORPH3US, wesleytorres1, Sir Mijael, Adrian_Yaj08 (w10 WATCH novice-deference / small sample) did not return in w11 cohort -- their WATCH calls validated as correct too.
- 2026-07-20: **FoxMilard heavy-grinder flavor-change BAN** is a new precedent case. Lifetime 62 prizes (Gold 21+Silver 14+Bronze 27) + LB Rank 27 normally reads as legitimate grinder; rules 3/6 novice-deference/sample-size defenses would shelter. But this cycle's flavor shifted to 4N pure NOOBS with 4 climb-and-cash cycles and 4 MIDDLES->NOOBS drops -- **rule 1 flavor-shift override applies**, veteran deference does not shelter cases with active flavor change to NOOBS. Trial conf 9/10. Analogous to Kacumi w6->w7 and TR-dennisfb w7->w8 pattern but with a much heavier veteran profile.
- 2026-07-20: **Rule 6 in-window vs lifetime ambiguity FLAGGED for post-cycle clarification**. La_Iena_River_ (PS Lifetime 54, in-window TotalPrizes 6, 42 NS 84% EXTREME with PCR 591 -> 140 crash) triggered rule 6 novice-deference on `TotalPrizes < 10` in-window branch, resulting in WATCH conf 6 despite extreme signature. Original KingYakO2 refinement (w8) referred to "Lifetime 7 total prizes ever" but current rule wording reads ambiguous. **On the standard wide-cohort where TotalPrizes gates are 4-11 by design, rule 6 fires broadly** on cases that should probably not be novice-deferred. Recommend clarifying rule 6: **"Lifetime prizes < 10" is the KingYakO2-intent threshold; in-window prizes are a separate factor**. To discuss with CS lead.
- 2026-07-20: **Rule 9 vs rule 6 collision FLAGGED for post-cycle formalization**. TurboBandz6351 (Xbox) hit ALL rule 9 within-bracket detector gates (NS 12/46%, pure 5N, max PCR 81 < 100, Reg 29 >= 10) AND rule 6 novice-deference (Lifetime 5 < 10). Trial resolved via rule 6 override (novice-deference prevails) + rule 8 persistence clock (auto-BAN if pattern persists). Currently implicit precedence; formalize explicit rule: **rule 6 wins over rule 9 on first-cycle NEW candidates, rule 8 persistence carries the escalation on the clock**.
- 2026-07-20: **DraVexTab (PS) w12 escalation candidate flagged**: PS weekly LB #5 with 4 wins at PCR 27, 8N+1M mixed flavor, Lifetime 15 borderline. Trial WATCH conf 7 this cycle but signal-adjacent to farmer profile -- rule 8 persistence check will fire next cycle if pattern maintains. **BB_Anastasia (Steam)** post-ban rehabilitation observation: our week-8 BAN expired 2026-07-13, returned to Steam weekly LB #1 with 7 wins but did not trigger the no-show gate this window -- signal of clean post-ban behavior, not action-worthy. **FarantirPL (Steam)** at LB #9 with 3 wins PCR -157, dropped out of cohort w10-w11 but continues surface-level LB extraction -- walking the gate boundary.
- 2026-07-20: Week-11 handoff delivered. C&S report `cs-report-2026-07-19.md` + per-platform TSVs (7 rows Steam: 1 BANNED by us + 2 BANNED by Support + 4 WATCH / 11 rows PS: 3 BANNED by us + 3 BANNED by Support incl. 1 trial-dissent + 5 WATCH / 2 rows Xbox: 1 BANNED by us + 1 WATCH). 20 TSV rows total (10 BANNED + 10 WATCH). Slack to CS lead sent in the compact conversational register; explicitly flagged 2nd Support dissent (LZ23J7KS as rule 7 direction 2 validation), evgeniy3311 rule-6 ladder second validation, FoxMilard veteran flavor-change, Belion019 cheat-orthogonal. Posted the weekly JIRA summary as comment 131297 (bans + funnel + Support cross-check + rule-6 ladder validation + watchlist + weekly LB sanity check).
