# FP-44591 — Backlog

- [ ] Implement FP-44590 first (unify Twitch client → shared `GetUserInfo`).
- [ ] Prod sizing: count `TwitchEmail IS NULL` links per stack to set the daily backfill sweep batch size (needs prod access).
- [ ] Confirm the WebAdmin Player Card / linking-page wording with CS before shipping (non-categorical "no e-mail / account may be unconfirmed").
- [ ] Decide final UX copy for the linking-page warning panel + "now confirmed" success message.
