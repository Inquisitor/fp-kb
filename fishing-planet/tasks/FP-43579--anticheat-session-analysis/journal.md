---
status: completed
executor: Stanislav Samoilov
jira: https://fishingplanet.atlassian.net/browse/FP-43579
---
# FP-43579: AntiCheat Game Session Analysis

## Status
**Closed 2026-05-12.** Tool live in admin under `/Anticheat/GameSessionAnalysis`. Code shipped LBM r16063, merged into MFT (Code) r16064. KB promotion: 3 feedback + 1 reference items registered in `<kb>/CLAUDE.md`; `web-admin` umbrella module created at [`<kb>/fishing-planet/server/modules/web-admin/`](../../server/modules/web-admin/) with deep-dive [`embedded-vue-pattern.md`](../../server/modules/web-admin/embedded-vue-pattern.md) as template for future SPA-style admin tools. All 14 implementation subtasks done; TST-001 (DAL paging tests) explicitly deferred post-v1 — symmetric to controller/model unit tests already deferred; no fixture infrastructure in `Sql.MsSql.Tests` to justify v1 scaffolding cost. Cross-link in FP-43424 backlog seeds the future `logging` module with FP-43579-surfaced findings (Mongo log semantics + LogBase pushdown). Plan at [artifacts/plan.md](artifacts/plan.md), per-subtask files in [artifacts/subtasks/](artifacts/subtasks/).

## Summary
WebAdmin tool for moderators to inspect player game sessions for anti-cheat patterns. Linked from Player → Moderation (`Cheat` page) and accessible via top-level URL `/Anticheat/GameSessionAnalysis`. Goal: visualize a player's clicks (TakeClick / ReleaseClick) over a time range as a heatmap on top of player's screenshot, surfacing bot signatures (LureKing hardcoded coords, window-center clusters) that are otherwise hidden in the merged log.

Iteration goal: cleanly designed backend (thin controller + Models per WebAdmin convention, no Services layer) and frontend (Vue 3 + TS + Vite, embedded as island in Razor host, no UI kit, scoped styles) with reusable building blocks — no inline boilerplate.

## Background — investigation findings

A pre-task investigation (prototype Python heatmap renderer) established the analytical patterns this tool will operationalize:

- **TakeClick / ReleaseClick coordinates** are written to Mongo `fishingLog` by the client via `Mouse.current.position.ReadValue()` at the moment the handler fires — whether triggered by mouse click, hotkey, or programmatic invocation. Coordinates are pixel-integer (Windows mouse pixel space)
- **Honest player**: clicks scatter across the actual KEEP / RELEASE button area on screen
- **Pattern A — LureKing** (confirmed bot signature, primary threat): hardcoded screen coordinates — `(663, 89)` for KEEP, `(473, 89)` for RELEASE — identical to the pixel across multiple unrelated accounts (sample: `LUYA168`, `rrsrewr`). Calibrated against a non-standard ~1136-wide window. No plausible non-bot explanation: two unrelated accounts cannot independently produce identical sub-pixel coordinates. Distribution and technical details captured in [artifacts/lureking-notes.md](artifacts/lureking-notes.md).
- **Pattern B — window-center cluster** (suspicious, attribution open): clicks concentrate at approximately `(window_w/2, window_h/2)` in client coords (sample: `W_CHUANQI`, `Niepan.LD`, `DFT_KennPF`, `adidan`). Two competing hypotheses, neither yet ruled out:
  - bot programmatically positions cursor before each handler invocation
  - Unity auto-centers cursor on some event (screen change / focus loss) and controller-only play leaves it there
  - A cluster outside KEEP/RELEASE button areas is **suspicious** regardless of cause and warrants per-case investigation. The tool is being built specifically to make this attribution feasible.

Catch panel UI geometry constants, scaling rule (match-width), recalibration guidance, and notes on why the empirical method is a stopgap (the proper approach is reading the prefab in Unity Editor) — see [artifacts/ui-geometry-calibration.md](artifacts/ui-geometry-calibration.md).

## Plan

Phases describe the workflow pipeline. Inside each phase, individual work items use 3-letter category prefixes (per FP-41746 convention): **ARC** = architecture decision, **RES** = research / spike, **DAT** = data access layer, **BCK** = backend C#, **BLD** = build pipeline, **FRT** = frontend Vue, **DOC** = documentation, **TST** = tests. Subtask IDs are KB-internal and never appear in commit messages or external docs.

- [x] **Phase 1 — Requirements doc** — see [artifacts/requirements.md](artifacts/requirements.md). v1 scope, future phases, data sources, UI behaviors, server architecture sketch, frontend stack candidate, open hypotheses, long-term goal. Concretization happens in subsequent phases.

- [x] **Phase 2 — Architecture doc** — see [artifacts/architecture.md](artifacts/architecture.md). All architectural decisions captured (closure table at top of doc). RES-001 verified by working scaffold spike; RES-002 parked as user task (not v1 blocker).
  - [x] **RES-001** — Vue 3 build pipeline spike. Closed by live scaffold + Razor embed verification. Findings: Vuetify (not Vue) was root cause of past `TargetedAdsPlanningTool` embed failure; no UI kit + scoped styles solves it cleanly.
  - [x] **RES-002** — Read canonical UI geometry. Done via `uloop` CLI runtime measurement (not prefab YAML — runtime overrides differ from prefab). Canvas: HUD_UI ScreenSpaceOverlay 1920×1080 reference, ScaleWithScreenSize + Expand. KEEP=PriorityButton, RELEASE=Button. Values in `WebAdmin/WebAdmin/Components/AntiCheatTool/src/calibration/uiGeometry.ts` (in SVN repo), reconciled with LureKing empirical at 1136-wide.
  - [x] **ARC-001..006** — all decided. See [architecture.md](artifacts/architecture.md) closure table.

- [x] **Phase 3 — Subtask breakdown** — see [artifacts/plan.md](artifacts/plan.md) and [artifacts/subtasks/](artifacts/subtasks/). 14 subtasks across 6 vertical slices (VS0..VS5). VS0 (architecture retention correction) done at decomposition time.

- [ ] **Phase 4+ — Implementation** — execute subtasks per [plan.md](artifacts/plan.md). After each: review, integrate, update journal milestones and backlog. New findings during implementation either open new subtasks or fold into existing ones.

## Milestones

- 2026-05-02: JIRA FP-43579 created. Pre-task prototype investigation completed (heatmap renderer + cheat signature analysis on 6 sample players). Initial WebAdmin scaffolding written, then reverted — restart with proper architecture. KB task opened. Requirements doc drafted — v1 scope, future phases, open hypotheses captured. LureKing identified as primary threat (Pattern A), notes captured in [artifacts/lureking-notes.md](artifacts/lureking-notes.md).
- 2026-05-03: Phase 2 (architecture) closed. [artifacts/architecture.md](artifacts/architecture.md) committed — covers stack (Vue 3 + TS + Vite, no UI kit), Area `Anticheat` with `GameSessionAnalysisController`, 3 AJAX endpoints, DAL variant B for `GetPlayerScreens`, component tree (`ScreenshotStrip` + `HeatmapView` + `CalibrationPanel`), localStorage schema (`anticheat:gameSessionAnalysis:monitorCalibrations`, LRU 100), Kendo CSS-class strategy + jQuery-bridge for Dropdown. RES-001 verified by working scaffold spike: 62 KB main.js, 0.4 KB CSS, admin chrome wraps Vue island cleanly, `_ViewStart.cshtml` gotcha for Areas documented. Scaffold files committed in WebAdmin (`Components/AntiCheatTool/`, `Areas/Anticheat/`, `Scripts/vue/anti-cheat/`).
- 2026-05-03: RES-002 closed (Phase 2 fully done). Canonical UI geometry measured at runtime in Unity Editor play mode via `uloop` CLI (not prefab YAML — runtime overrides via code). Host: `HUD_UI` Canvas (ScreenSpaceOverlay, 1920×1080 ref, ScaleWithScreenSize + Expand). Buttons: KEEP=PriorityButton (canvas X 980-1230), RELEASE=Button (canvas X 690-940), both Y 92-154. Catch panel: 610-1310 × 18-408. Values written to `Components/AntiCheatTool/src/calibration/uiGeometry.ts`. Verified against 8 real-world investigation samples with two complementary metrics: (a) `% TakeClicks in KEEP-rect` catches button-targeting bots (LureKing LUYA168/rrsrewr 99-100% vs all others ≤17%); (b) `centroid distance to W/2 H/2` across popular resolutions catches window-center clusters (Pattern B detected even on 6-click sparse data via centroid stability). **Steam Deck controller-only honest player (Jangalor) is a confirmed false-positive for window-center metric** — 17/17 clicks at exactly (640, 399) in 1280×800 window, distance 1.0 px, variance 0 px. Falsified earlier hypothesis that cluster tightness discriminates bot vs controller — both produce equally tight clusters. Working discriminators (for Phase 4): hardware signature in `diagSysInfoLog`, Releases distribution at button vs center, click rate, manual moderator review with account context. v1 verdict logic for window-center match is **suspect, not auto-bot** — surface to moderator. Multi-cluster case (adidan) revealed limitation of single-centroid analysis — Phase 4 anomaly scoring must do per-cluster decomposition (DBSCAN). v1 tool's primary value is **visual moderator inspection**. Verification script at `artifacts/verify_lureking_runtime_geom.py`. `artifacts/ui-geometry-calibration.md` marked superseded.
- 2026-05-03: Phase 3 (subtask decomposition) done. [artifacts/plan.md](artifacts/plan.md) committed — 14 subtasks across VS0..VS5, vertical-slice ordering with each slice leaving tool demo-able. Per-subtask files at [artifacts/subtasks/](artifacts/subtasks/). VS0 (DOC-000) executed in-flight: architecture's «Data availability constraints» section reworked — `fishingLog` 14d (out-of-band SoT), `diagSysInfoLog` retention unverified (no TTL/cleanup-job in repo), `Stats.Screens` 14d (verified `SqlAnalyticsProvider.ScreenStorageHorizon`); UI retention banner removed from v1 to avoid hardcoded misleading claims. Backlog updated: DAT-001 preconditions resolved (1 impl, 0 mocks, retention 14d for SQL); new DOC-003 «KB logging module promotion» captured as out-of-scope follow-up.
- 2026-05-03: VS1 (end-to-end skeleton) code-complete and partially smoke-tested. Backend: `MongoSysInfoProvider` got `FindActiveDuring(userId, start, end)` (renamed during smoke after Find-with-range returned empty for a user whose only SysInfo record predated the 14d default window). Key insight: `diagSysInfoLog` is **stateful snapshot semantics** (config active from record-time onwards) — not event-stream semantics like `diagErrLog`. The original `Find(userId, start, end)` clone of `MongoErrorProvider.Find(userId, start, end)` was wrong by reflex; semantic mismatch caught by smoke. Carry-over algorithm: latest record `<= start` + records in `(start, end]`, asc-sorted. Same pattern likely applies to `IpLog` / `MacAddressLog` (also stateful) — to be captured in DOC-003 (logging module promotion). Frontend: `_PlayerToolsPartial` was missing from `Index.cshtml` despite being in the architecture doc — added; `Request["userId"]` flows through. Default range: `Index` action defaults to `[UtcNow.AddDays(-14), UtcNow]` when only `userId` is supplied. Razor parser CS1525 with `?.` + double-quoted format string inside `@(...)` — fixed by extracting `fromValue` / `toValue` into `@{ ... }` block. Link added in `_PlayerToolsPartial` (per-player, Moderation section) AND `Player/CheatContent.cshtml` (global tool list, no userId).
- 2026-05-03: VS2 (events visualisation) code-complete. **BCK-002**: `Events` endpoint reads `fishingLog` via `new LogBase(LogCollection.FishingLog).Find(userId, from, to)` (event-stream semantics — correct here, distinct from VS1's stateful SysInfo case), parses `TakeClick`/`ReleaseClick` coords via compiled Regex, applies 10k cap (last-N by timestamp desc, then re-sort asc) + `truncated` flag. **FRT-003**: `HeatmapView` renders 3 layers on `<canvas>` — bbox stroke, KEEP/RELEASE/catchPanel rects (Expand-mode scaling from `uiGeometry.ts`), click points (Y-flip Mouse.position → canvas; Take green / Release red, 3px circles). `watchEffect` re-renders on any reactive change. **FRT-004**: full `CalibrationPanel` — preset dropdown (monitor-extracted ∪ curated [1280×720, 1920×1080, 2560×1440, 3840×2160]), manual mode (width slider 800-3840 + AR radio over `STANDARD_ASPECT_RATIOS`), offsetX/Y sliders ±300px, Take/Release toggles. `aspectRatios.ts` extracted; HeatmapView uses `aspectRatioHeight()` instead of inline 16:9 fallback. Single `update:calibration` emit with full new object (rejected separate per-field events as boilerplate w/o gain). **FRT-005**: `usePlayerCalibration(userId)` composable — localStorage key `anticheat:gameSessionAnalysis:monitorCalibrations`, schema v:1, LRU 100, `ts` updated on read+write, debounced save 200ms trailing (slider drag would otherwise hammer storage at 60fps). userId switch flushes pending save before reload. App.vue swapped from local placeholder ref to composable. Build: 25 modules, main.js 74.4 KB / style.css 2.6 KB. yarn type-check: 0 errors.
- 2026-05-03: VS2 smoke iteration. (1) **0 clicks bug**: anchored `^(?<kind>...):\s+(?<x>...);\s+(?<y>...)$` regex was too strict — relaxed to non-anchored substring match (`re.finditer`-style, mirroring `heatmap_gen.py` EVENT_RE). Filter switched from `StartsWith("TakeClick")` to `EventRe.IsMatch(message)`. (2) **Heatmap visual rework** per user pointer to `heatmap_gen.py` aesthetic: dark canvas (#0d0d0d), fine axis grid with pixel labels every ~step (heatmap_gen.py step formula ported verbatim), orange center-cross, dashed UI rects @opacity 0.55 (KEEP=orange, RELEASE=blue, panel=grey), dashed frame border. Take = filled cyan-blue (`hsl(200,80%,55%)`) with thin white stroke, Release = no fill + dashed red ring — kinds visually distinct without colour-blindness ambiguity. Build: 25 modules, main.js 75.65 KB.
- 2026-05-03: VS2 smoke iteration #2 — **dashed UI rects invisible at high resolutions**. Root cause: SVG-vs-canvas distinction. SVG (heatmap_gen.py) renders strokes in device-px regardless of viewport; canvas is raster, so a 1-canvas-px stroke at 3840×2160 displayed at ~800 CSS-px = 0.21 CSS-px wide → antialiased into nothing. Same problem with axis labels (9px font on 3840 canvas = ~1.9 CSS-px tall, illegible). Fix: introduced `pixelScale(w) = max(1, w / 1920)` and multiplied all strokes/fonts/dash-lengths/click-radii by it. For 1920 → ×1 (unchanged), for 3840 → ×2 (visible). Also bumped raw line-widths (axis 0.5→1, UI rects 1→2, opacity 0.55→0.7) for general legibility regardless of resolution. Build: main.js 75.83 KB.
- 2026-05-03: VS2 smoke iteration #3 — **hatched placeholder for empty-screenshot mode** (not in original architecture, added per smoke feedback). Diagonal 45° lines (`#1c1c1c` on `#0d0d0d`, 18×pixelScale step) fill canvas Layer 0 when `activeScreenshot == null`. Reads as «no screenshot here, by design» — pure empty bbox alone was ambiguous (broken-render vs intentional). FRT-007 (heatmap-on-screenshot) will skip this draw and use `ctx.drawImage` in the same Layer 0 slot.
- 2026-05-03: VS2 smoke iteration #4 — **reverted iteration #2 pixelScale on grid/frame/center/clicks** at user request (those looked ideal at original thinness). pixelScale kept ONLY where it was actually load-bearing: dashed UI rects + their labels (which were the real problem at high resolutions). Hatched background also keeps its scale (visual texture).
- 2026-05-03: VS3 (screenshots paged) code-complete. **DAT-001**: `IAnalyticsProvider.GetPlayerScreens` extended (variant B per architecture) with optional `from / to / skip / take`; `GetPlayerScreensCount` added. SQL built via `StringBuilder`, clauses appended only when each param non-null. Existing single-arg call site (`ScreensModel.cs:15`) unchanged via default args; no Mock setups affected. **BCK-003**: `Screenshots` action — page/pageSize clamped server-side (1..10000 / 1..100), reuses `ValidateInputs` + `JsonResponse(model.Data)`. **FRT-006**: `ScreenshotStrip` with thumbnail row (`<img loading="lazy">`, active border `#3e80cf`), Prev/Next pager + page indicator, `pageSize` lifted to App.vue prop. Click thumbnail toggles `activeId` (second click deselects). **FRT-007**: `HeatmapView` async-loads `<img>` for active screenshot via `watch(() => props.activeScreenshot, …, { immediate: true })`, `onCleanup` cancels in-flight loads on rapid switches. drawHeatmap branches: image set → `ctx.filter = 'brightness(0.55)'` + `ctx.drawImage(0, 0, w, h)` + `ctx.filter = 'none'` reset (dimming mirrors heatmap_gen.py to keep click overlays readable over bright lake/sky photos; reset is critical or filter leaks into subsequent draws), null → existing hatched placeholder. Build: 25 modules, main.js 77.32 KB.
- 2026-05-03: Post-VS3 smoke iteration — `ctx.filter` dimming was invisible to user; switched to `ctx.fillRect(rgba(0,0,0,0.45))` overlay after drawImage (more portable, no leaked-state risk). Filter form layout switched from vertical `<table>` to horizontal flex row (userId + From + To + Apply in one line) — saves ~3 lines of vertical space.
- 2026-05-03: VS4 (polish) code-complete. **FRT-008**: error reporting via singleton `useErrorReporter` (mirrors useRefreshSignal pattern). 4xx → dismissible banner; 5xx → `<iframe sandbox srcdoc>` overlay rendering Razor error page (XSS-safe, no parent access). Truncation banner for `eventsTruncated` (Events 10k cap) — soft yellow warning. Loading skeletons explicitly deferred per architecture. **FRT-009**: `KendoDropdown.vue` jQuery widget bridge per architecture template — single `<input ref>`, `widget.destroy()` in onBeforeUnmount, write-through `watch(modelValue)` with same-value guard, deep `watch(options)` for late-arriving monitorInfo, empty-string ↔ null boundary conversion. Stop-criterion not triggered; one-line CalibrationPanel revert path if popup breaks. Bundle 28 modules, main.js 79.26 KB. **DOC-001**: `Components/AntiCheatTool/README.md` — 9 sections per architecture (description, architecture, file structure, build cmds, dev workflow, csproj integration, initial-state pattern, Kendo bridge gotchas, TargetedAdsPlanningTool contrast table).
- 2026-05-03: Post-VS4 smoke iteration — **`<header>` 135 px global leak from `Content/Layout.css`** (`header { height: 135px }` site-wide). Vue scoped styles isolate OUR rules, not the element from globally-cascading rules. CalibrationPanel / HeatmapView / ScreenshotStrip used `<header>` for their title bars → got 135 px reserved space above content (visible as huge gap between «Calibration» / «Heatmap» / «Screenshots» titles and their first row). Fixed by replacing `<header>` with `<div class="...-header">` in all three components. Documented as a project-wide rule in `Components/AntiCheatTool/README.md` (avoid bare semantic tags `<header>/<footer>/<aside>/<main>` inside Vue islands — admin's global CSS preempts scoped styles).
- 2026-05-03: Post-VS4 smoke iteration — **Events endpoint slow on active players** (user reported 30 min wait on 2797-click query). Root cause: `LogBase.Find(userId, from, to)` returns ALL fishingLog entries for the user/range (cast/bite/fish/take/release/...), with the prefix-only filter applied client-side in C# — for active players the cursor enumerates hundreds of thousands of entries, deserialising each from BSON. Two fixes: (1) added `LogBase.Find(userId, from, to, BsonRegularExpression)` overload that pushes `Query.Matches("Message", regex)` to Mongo so the cursor returns only matching docs; (2) reduced `MaxEvents` cap from 10000 → 1000 (smaller render workload). EventsModel now uses the new overload with `BsonRegularExpression("TakeClick|ReleaseClick")`. Bonus: also extends to other heavy fishingLog query patterns reusing the same `LogBase` API.
- 2026-05-03: Post-VS4 smoke iteration — `usePlayerCalibration` now exposes `isPersisted` so App.vue can auto-apply monitor-detected resolution as starter calibration for fresh users (e.g. Jangalor 4K monitor → preset 3840x2160 instead of 1920x1080 default). Persisted entries are respected (Steam Deck false-positive case kept its manual override). Default resolution preset list extended from 4 to 19 entries mirroring `heatmap_gen.py STANDARD_WINDOWS` (4:3 / 16:9 / 16:10 / 5:4 / 21:9), sorted by width.
- 2026-05-03: Post-VS4 smoke iteration — canvas display capped at `max-height: 80vh` (preserves AR via auto width/height + max-width 100% + max-height 80vh) — heatmap no longer scrolls off-screen on 4K calibrations.
- 2026-05-03: Post-VS4 smoke iteration — **comma decimal separator in TakeClick/ReleaseClick coords** (e.g. `TakeClick: 955,000; 539,000`) — appears when the client logs under a locale that uses `,` as decimal. Original regex `[\d.]+` rejected such messages silently. Fix: regex extended to `[\d.,]+`, parser does `Value.Replace(',', '.')` before `double.Parse(InvariantCulture)`. heatmap_gen.py has the same gap (separate codebase, out of scope here).
- 2026-05-03: Post-VS4 smoke iteration — **click aggregation by exact pixel** ported from heatmap_gen.py. Repeat clicks at the same (x, y) per kind grow visibly: radius `2.5 + √count × 1.3`; take fill color HSL(240→0) log-scaled by max-count (single click blue → densest red). Sort ASC by count so densest clusters draw on top instead of being buried under singletons. Release rings get same radius growth but keep `#ff5252` constant — kind-color invariant for moderator's eye.
- 2026-05-04: Smoke-driven UX additions: hover crosshair (yellow lines + 14×14 even-sized canvas-pixel reference box centered on integer (hx, hy)), CSS-pixel-constant circle radii via `cssToCanvas` factor + ResizeObserver, `displayMode` toggle ('Exact pixels' | 'Group 3×3') in CalibrationPanel, Stats fieldset (Total/3×3 compact/% in compact per kind, orange highlight when share > 50%), Top-20 hot-points table (Count / % / Kind / X / Y) with hover→crosshair binding. Calibration auto-applies monitor-detected resolution as starter for fresh users (preserves persisted overrides). Resolution preset list extended to 19 standard windows mirroring heatmap_gen.py. Canvas capped at 80vh, scroll-to-bottom on resolution change. Right-side 10 px padding compensating admin Layout asymmetry. Fine-tune offset sliders collapsed under toggle by default.
- 2026-05-04: TST-002 smoke pass confirmed by user. TST-001 deferred to post-v1 (no AnalyticsProvider fixture; symmetric to architecture's already-deferred controller/model unit tests). Pre-commit cleanup: hardcoded 2026-05-01 default replaced with `DateTime.UtcNow.Date.AddDays(-1)` (yesterday 00:00 UTC). Phase 4 closed; SVN commit pending.
- 2026-05-04: Late polish iteration before commit — fixes that surfaced during extended user smoke. **Area routing — known limitation**: tried `controller` regex constraint on Anticheat_default to block non-Anticheat controllers during URL generation; instead it broke ALL outgoing links from Area context (empty `<a href>`). Reverted. The remaining fix is `namespaces`-only (incoming protection); shared `_Layout` ActionLinks still produce `/Anticheat/...` 404 from inside Area context (ASP.NET MVC ambient `area` strand on the root Default route). Documented as known v1 limitation in README; proper fix (move controller out of Area, custom route) deferred. **Crosshair sizing**: center cross arms / crosshair box / line widths now all `× cssToCanvas` (constant CSS px regardless of calibration). `pixelScale(w)` helper retired; `cssToCanvas` is the universal logical-per-CSS factor used everywhere except hardcoded grid lines (kept thin per architecture's grid-aesthetic preference). Backing-buffer upscale (`renderScale = max(1, 2560/w)`) keeps small calibrations sharp on HiDPI. **Expand-mode UI offset**: catch panel + buttons are bottom-anchored (Y values 18/92), so only horizontal centering offset (`uiOffsetX`) is needed; the Y rendering stays anchored to canvas bottom. Earlier offsetY caused regression on 4:3 — reverted. **Filter form**: switched to single-row inline-flex layout with each label-input pair in its own inline-flex (label and input on same line), `font: 100%` on submit input matches container size. **Layout/UX**: padding-right 10 px on app root (compensates admin `#mainBody` only-left-padding), `#playertoolspanel` page-scoped override `top: 0`. Click circle radii compensated via `cssToCanvas` for HiDPI.
- 2026-05-03: Post-VS4 smoke iteration — **shared admin links 404 from Area context**. `Html.ActionLink("My Players", "MyPlayers", "Account")` in `_LoginPartial.cshtml` etc. generated `/Anticheat/Account/MyPlayers` when invoked from any view inside Anticheat Area — root controllers don't exist in Area, click → 404. Fix: add `namespaces` constraint to AnticheatArea route — `new[] { "WebAdmin.Areas.Anticheat.Controllers" }`. Without the constraint, the Area route accepts any controller for URL generation; with it, the URL generator falls back to the root Default route for non-Area controllers and produces the correct `/Account/MyPlayers`. Documented as mandatory rule in `Components/AntiCheatTool/README.md`.
- 2026-05-04: **ARC-007 resolved in v1 (not deferred): MVC Area dropped, custom route used.** Earlier session iterations on the Area route (namespaces-only, then namespaces + controller constraint) couldn't fix outgoing URL generation from inside Area context: namespaces-only kept `/Anticheat/Home/...` 404 links, controller constraint broke ALL outgoing links (empty `<a href>` because ambient `area=Anticheat` stranded on root Default route). Final fix delegated to a senior-eng agent: removed Area entirely. Files moved via `svn move`: `Areas/Anticheat/Controllers/GameSessionAnalysisController.cs` → `Controllers/Anticheat/GameSessionAnalysisController.cs` (ns `WebAdmin.Controllers.Anticheat`); 4 models → `Models/Anticheat/GameSessionAnalysis/`; view → `Views/GameSessionAnalysis/Index.cshtml`. `Areas/Anticheat/` deleted (incl. `_ViewStart.cshtml`, `Web.config`, `AnticheatAreaRegistration.cs`). Custom route registered ahead of Default in `App_Start/RouteConfig.cs`: `MapRoute("Anticheat", "Anticheat/{controller}/{action}/{id}", defaults, constraints: new { controller = "GameSessionAnalysis" }, namespaces: new[] { "WebAdmin.Controllers.Anticheat" })`. URL prefix `/Anticheat/...` preserved; no Area = no ambient `area` token = no strand on Default. Smoke pass confirmed: `/Anticheat/GameSessionAnalysis?userId=...` works; AJAX endpoints work; shared-layout `Html.ActionLink("Home"...)` from inside the tool produces `/Home/Index` cleanly. README updated with Routing section explaining why this isn't an Area; future tools under `/Anticheat/...` extend the controller regex.
- 2026-05-04: **popstate Back-button bug fix.** popstate handler read URL only and dispatched refresh with whatever was in URLSearchParams. Initial entry URL has only `?userId=...` (server defaulted from/to weren't reflected in URL). After Apply → URL had all 3 → Back returned to URL with only userId → dispatch fired with `from=""`, `to=""` → main.ts listener rejected as "incomplete detail" → form stayed stale, no reload. Fix: popstate now (1) mirrors the server's `DateTime.UtcNow.Date.AddDays(-1)` default range client-side when URL is missing from/to, (2) syncs the form's text input, hidden inputs, AND Kendo widget values via `widget.value(new Date(...))` so the visible form matches the URL being navigated to, (3) dispatches refresh with full detail. User-confirmed working 2026-05-04.
- 2026-05-04: **Deep review (feature-dev:code-reviewer agent) found 1 blocker + 3 high + 3 medium + 3 low; blocker/high fixed before commit.** (1) `Guid.TryParse` added to `ValidateInputs` — non-GUID userId hit `new Guid(userId)` in ScreenshotsModel and threw FormatException → unhandled 500. Now returns 400 cleanly. (2) `popstate` `new Date(from)` was parsing UTC string as local time per ECMA-262 (no TZ designator → local) → Kendo widget showed wrong time after Back navigation by browser's UTC offset; fixed via `parseUtc(s)` helper that converts `"yyyy-MM-dd HH:mm:ss.fff"` → `"yyyy-MM-ddTHH:mm:ss.fffZ"` before `new Date(...)`. (3) `watchEffect` was registered inside `onMounted` in HeatmapView — Vue couldn't auto-stop on unmount (effect outside `EffectScope`); moved to `setup()` scope. (4) `ApiError` body-handling refactored: read body ONCE via `cloned.text()`, then attempt JSON.parse on the text; previous code had a fallback `await res.text()` after `await res.json()` already consumed `res`. Stale doc "10k cap" → "MaxEvents cap". Medium findings (Mongo regex unanchored, calibration redundant persist on userId switch, refresh signal duplicate trigger) folded into backlog as low-priority follow-ups.
- 2026-05-05: **Committed as LBM r16063.** SVN commit covered Mongo + DAL + WebAdmin (controller / models / view / Components/AntiCheatTool / Scripts/vue/anti-cheat / RouteConfig + 2 modified shared views + csproj). Per-file scope explicit to exclude unrelated WIP files in working copy. Cross-branch merge to MFT20260325 (Code role) pending — r16063 > MFT-base 15942, explicit merge required (no branch-copy inheritance).
- 2026-05-05: **Merged LBM r16063 into MFT (Code branch) as r16064.** Clean merge — no conflicts (all new files + 3 modified shared views diverge cleanly).
- 2026-05-12: **Four FP-wide KB items promoted from this task's findings.** Feedback: [`mongo_log_semantics.md`](../../../feedback/mongo_log_semantics.md) (stateful-vs-event log semantics), [`vue_island_bare_semantic_tags.md`](../../../feedback/vue_island_bare_semantic_tags.md) (avoid bare `<header>` etc. in Vue islands inside WebAdmin), [`aspnet_mvc_area_ambient_strand.md`](../../../feedback/aspnet_mvc_area_ambient_strand.md) (don't use MVC Areas for tools sharing `_Layout` ActionLinks). Reference: [`mongo_logbase_pushdown.md`](../../../reference/mongo_logbase_pushdown.md) (`BsonRegularExpression` overload for heavy log queries). Registered in `<kb>/CLAUDE.md` Feedback Rules / References. FP-43424 backlog updated with cross-link — when `logging` module is created/deepened, fold mongo_log_semantics + mongo_logbase_pushdown into the module card / deep-dive.
- 2026-05-12: **DOC-002 — `web-admin` module created as umbrella stub.** Path: `<kb>/fishing-planet/server/modules/web-admin/`. Files: [`_card.md`](../../server/modules/web-admin/_card.md) (status `stub`, umbrella style, conventions/gotchas firm, controller inventory TBD pending FP-43424 Pass 2/3), [`log.md`](../../server/modules/web-admin/log.md), [`embedded-vue-pattern.md`](../../server/modules/web-admin/embedded-vue-pattern.md) — full deep-dive: tech stack rationale, project layout, initial-state injection, CustomEvent refresh + popstate, routing (custom route, not MVC Area), Vite build + csproj registrations, backend layering, state-management with composables-as-store, Kendo CSS classes + jQuery widget bridge, admin-layout constraints, performance budgets, new-tool checklist, TargetedAdsPlanningTool contrast table. Registered in `<kb>/fishing-planet/server/_index.md` under new section «Operations & Admin».
- 2026-05-12: **Task closed.** `status: completed`. Journal Status section rewritten to final summary. Removed from `_index.md` Active Tasks. Bubble-up to `web-admin` module backlog: code-review medium findings (Mongo regex unanchored, calibration redundant persist, refresh-signal dedup) — surfaced from FP-43579 deep code review 2026-05-04, low-priority, revisit on related-code touch.
