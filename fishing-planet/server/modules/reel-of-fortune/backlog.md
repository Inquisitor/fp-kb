# Reel of Fortune — Backlog

- [ ] **Re-examine `adapter.Init()` in `WebAdmin/Models/Tools/ToolsModel_Fortune.InitReelOfFortuneAdapter`.** It runs `Execute("Init", ...)` → `InitAvailableSpins(now)`, which silently rolls over `ReelOfFortuneState` on the offline profile (resets day-spin counters, increments `GoldenSpinDayNumber`, re-credits regular spins) when `today != LastCheckTime`. The bug fix it ships with (FP-41507) does not need it — the constructor's `Profile.ProductAdded` subscription is what credits premium spins on the subsequent event. The call appears to have been copied from the `InitClubAdapter` pattern. If it's not actually load-bearing, both this `Init()` call and `FortuneCache.InitDefault()` (added to `Global.asax.cs` to make `Init`'s cache reads work) could be trimmed. Author of FP-41507 no longer on team — verify next time anything in `ToolsModel_Fortune.cs` or the ROF offline path is touched. (Source: FP-41507 review, F-2.)
