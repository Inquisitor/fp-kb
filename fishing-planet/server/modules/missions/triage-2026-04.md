# Daily Missions — Release 2026.3 Triage

> **This file is a transient release-pass triage log.** Format below is strict — do not alter structure. See `memory/batch-triage-mode.md` for rationale.
>
> **Entry criteria (all three must hold):**
> 1. **Open** — not resolved inline during review (`Accept` / `Skip` are NOT in scope)
> 2. **Introduced by commit under review** — pre-existing gaps go to `modules/missions/backlog.md`, not here
> 3. **Requires a decision** — author clarification, patch, hotfix, or new ticket
>
> When in doubt: do NOT include. Better to miss than to drown the meeting.
>
> **Closed items move to the "Decided" section with a filled `Verdict` field.**

## Open

- **Source:** [FP-42372](https://fishingplanet.atlassian.net/browse/FP-42372) / [F-2](../../../review/FP-42372--daily-missions-preserve-completed/review.md), LBM r15868 — `Container_RefreshDailyMissions` completed branch bypasses `Container_RemoveMission`
  - **Finding:** The new completed-mission cleanup in `Container_RefreshDailyMissions` (`Shared/ObjectModel/Mission/MissionsManager.cs`) uses `AllMissions.Remove(missionToRemove)` directly. The existing helper `Container_RemoveMission` additionally cleans `MissionsReadyToStart`, `GlobalMissions`, hints, and monitoring registrations.
  - **Severity:** Low-Medium (latent)
  - **Code state:** fresh (~1 month)
  - **Proposed action:** Ask Yuriy — intentional (e.g., to avoid firing `MissionCancelled` event via `Core_RemoveStartedMission`) or oversight?
  - **Verdict:** TBD 2026-04-27

- **Source:** [FP-41909](https://fishingplanet.atlassian.net/browse/FP-41909) / [F-1](../../../review/FP-41909--daily-missions-debug-tools/review.md), LBM r15738 — `new char[';']` typo silently disables `;`-split in `SendMissionAdminActionToPlayer`
  - **Finding:** `Shared/SharedLib/Missions/MissionHelper.cs` → `SendMissionAdminActionToPlayer` calls `dependencies?.Split(new char[';'], StringSplitOptions.RemoveEmptyEntries)`. `new char[';']` is `new char[59]` of `'\0'` (semicolon implicitly converts to `int` for the array length), so no split occurs and `tunedDependenciesDynamic` receives `["a;b;c"]` instead of `["a","b","c"]`. Regression vs r15737 where the split lived in `MissionsManager_Admin.cs` and additionally `.Trim()`-ed and filtered empty entries. The Monitor UI in `WebAdmin/Views/Player/Missions.cshtml` is HTML-commented (`<!--td...<td-->`), so the path is unreachable today.
  - **Severity:** Low-Medium (latent)
  - **Code state:** fresh (~3 months)
  - **Proposed action:** Ask Yuriy — was the loss of `.Trim()` and `IsNullOrEmpty` filtering intentional, and is the Monitor UI planned to come back? If yes → patch (`new[] { ';' }` + restore Trim/filter); if no → Accept with caveat in `modules/missions/log.md` so re-enabling the UI does not silently ship a broken feature.
  - **Verdict:** TBD 2026-04-27

- **Source:** [FP-43186](https://fishingplanet.atlassian.net/browse/FP-43186) / [F-1](../../../review/FP-43186--daily-missions-tab-refresh-delay/review.md), LBM r15957 — `SaveProfileWithLog` removed from `GameClientPeer_Travel.cs` `GetTime` reverts FP-41909 WebAdmin-visibility fix; want to confirm mechanic understanding
  - **Finding:** In `Photon/.../GameServer/GameClientPeer_Travel.cs` `HandleTimeOperation` → `OperationCode.GetTime`, `SaveProfileWithLog("DailyMissionsGeneratedOnGetTime")` was replaced with `Mission.LogAsync(...)` (log-only). The save was added by the same author at LBM r15738 (FP-41909) to make freshly-generated daily missions visible in WebAdmin for online players (`PlayerDailyMissionsModel.Load()` reads from DB via `LoadPlayerProfile`, no in-memory fallback). Working hypothesis: removal pairs with the new `GenerationRefreshJitterSeconds` jitter as one anti-thundering-herd design — jitter spreads scheduler firings, and not saving avoids the midnight DB burst that ~10K concurrent players × multi-MB full-profile blob (`SavePlayerProfile` is a full-blob stored proc; in-code comment at `SqlProfileProvider.cs:143` already flags mass-save as a known pain point) would create. WebAdmin staleness for online players between regen and the next natural save event (level-up, balance change, mission completion, unload) is the trade-off.
  - **Severity:** Low
  - **Code state:** fresh (~3.5 weeks)
  - **Proposed action:** Ask Yuriy — is the mechanic understood correctly (jitter + suppressed forced save = anti-burst by design)?
  - **Verdict:** TBD 2026-04-27

- **Source:** [FP-41909](https://fishingplanet.atlassian.net/browse/FP-41909) / [F-2](../../../review/FP-41909--daily-missions-debug-tools/review.md), LBM r15738 — Dead `Admin_ClearDailyMissions` and empty-stub `Admin_RegenerateDailyMissions` left in `MissionsManager_Admin.cs`
  - **Finding:** Two new `private` methods at the end of `Shared/ObjectModel/Mission/MissionsManager_Admin.cs` are unreferenced anywhere in the tree (verified via grep on the symbol names). `Admin_ClearDailyMissions()` has a real body that iterates `AllMissions.Where(m => m.MissionId < 0)` and calls `Core_RemoveStartedMission`; `Admin_RegenerateDailyMissions(int? missionId)` is an empty stub returning `true`. The actual Clear/Regenerate flow lives in `DailyMissionAdapter_Admin.cs` and is dispatched through `GameClientPeer_Missions.ProcessMissionAction`. The empty stub is the more harmful of the two — a future reader scanning `MissionsManager_Admin.cs` for "how does daily-mission regeneration work" finds a no-op.
  - **Severity:** Low
  - **Code state:** fresh (~3 months)
  - **Proposed action:** Ask Yuriy — leftover WIP from the architecture pivot to `DailyMissionAdapter`, or were callers intended somewhere else? If leftover → delete both methods in a follow-up patch; otherwise wire up the intended call sites.
  - **Verdict:** TBD 2026-04-27

## Decided
